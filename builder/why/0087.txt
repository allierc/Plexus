time   2026-09-19 11:49:03
spec   plat_r6_water
camera azim=20.0 elev=8.0 zoom=1.15
movie  mp4/0087.mp4  (61.3 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/plat_r6_water/movie.mp4)
why    R6: water. 98,808 animal points and 140,000 seawater points scatter into ONE mpm_grid -- that sharing IS the coupling, since mpm_scatter zeroes the grid only on the substep's first scatter and accumulates thereafter. Nothing applies a force TO the water. This rung forced a real fix in the engine: mpm_viscosity published its viscous stress to the GLOBAL H.extra_stress, one slot shared by every particle set, so the water's 140,000-point stress was added to the animal's 98,808 and the run died on the first scatter. It is per-set now, as polar_active_stress already was, with the global kept as a fallback for one-cloud models; the same guard now protects the global active_stress slot. (after the run)
