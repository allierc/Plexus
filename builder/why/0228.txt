time   2026-09-22 13:37:48
spec   cil_s8_dense
camera azim=35.0 elev=12.0 zoom=1.3
movie  mp4/0228.mp4  (17.8 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s8_dense/movie.mp4)
why    TESTING THE COUPLING, NOT THE PARAMETERS (cil_s8_dense). The rod is 0.53% of the mass its own velocity is averaged with: a node weighs 7.3e-3 and scatters over a 3x3x3 stencil holding 1.39 of water. mpm_gather REPLACES a particle's velocity with the grid's mass-weighted average, so 99.5% of whatever the moment does in a substep is discarded before it returns. That is why clamp 40 flops the base to +107 deg while clamp 8e4 gives exactly 0.00 amplitude -- neither is reaching the rod, and the operator's own arithmetic is fine (a unit test gives the quasi-static 9.86 deg it should). It is the same finding as the first water run, where the base went 60.3 deg to 1.0. So this pair changes the ROD'S DENSITY and nothing else: at x100 the rod is 52.8% of its cells instead of 0.53%. A cilium is NOT a hundred times denser than water and this is not a proposal -- it is the experiment that says whether the coupling is the cause. If the beat appears, the fix is a drag-based exchange rather than velocity replacement, and the density goes back. Fast config so iterating costs minutes: 10 Hz over 200 frames is ten beats, and the sperm number only rises as omega^(1/4). (after the run)
