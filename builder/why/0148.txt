time   2026-09-22 09:29:56
spec   cil_s1_onerod
camera azim=35.0 elev=12.0 zoom=1.3
movie  mp4/0148.mp4  (8.3 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s1_onerod/movie.mp4)
why    STEP 1, centred, drawn as a filament, and now THREE operators where there were six. The rod sits at x=y=0.5 spanning z 0.3 to 0.7 -- dead centre of the unit box. The earlier shot that looked off-centre was the CAMERA (azim 90, elev 0), not the model; this one uses the default view. rod_stretch + rod_bend merged into rod_elastic, because stretching and bending are one material -- always declared together, same set, both internal. rod_pin + rod_base_moment merged into rod_base, because the direction clamp and the drive are THE SAME MOMENT ACROSS THE SAME JOINT: one takes a restoring law, the other a command. rod_drag deleted outright, because motion_ops.drag already existed and only lacked a tangent, so it gained 'along: chain'. The drag stays OUT of rod_elastic on purpose: everything in rod_elastic is an equal-and-opposite pair or a (-1,+2,-1) triple, while drag is a sink into a fluid that is not represented, and merging them would hide a momentum leak inside an operator whose name promises there is none. Behaviour-identical: base 60.297 deg, tip 31.753, tip/base 0.53, period 0.9526 s against a commanded 1.000, sperm number 1.49. (after the run)
