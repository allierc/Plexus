time   2026-09-22 08:53:41
spec   cil_s1_onerod
camera azim=90.0 elev=0.0 zoom=1.6
why    STEP 1 of the rebuild: ONE cilium, on a FIXED base, driven by a moment at that base, and nothing else in the scene so that 'does it beat' has one answer. A cilium as a DISCRETE ELASTIC ROD rather than MPM material: MLS-MPM carries one velocity per grid node, so a body thinner than a cell cannot move relative to the fluid it shares nodes with, and a real cilium is 0.25 um against a 4.08 um cell -- which is why rungs 13 to 17 kept widening it into a 6 um paddle and then a 25 x 10 um blade. A rod has no such floor: its nodes are points on a curve and its thickness is a drag coefficient, not a volume. Six new operators in src/plexus/operators/rod_ops.py; the one that was missing is rod_base_moment, a moment across the base JOINT whose reaction lands on the anchored node. RESULT: base joint swings 60.3 deg, tip 31.8 deg, tip/base 0.53, period 0.953 s against a commanded 1.000, centred at -1.7 deg and sustained over ten cycles, with the tip LAGGING the base so the stroke is curved. Sperm number 1.49, inside the 1-4 real cilia live at. Every constant was measured by a sweep, not chosen: drag 5.0 gave a DEAD tip because Sp was 4.3 and the stroke died a quarter of the way up the rod, and without the base DIRECTION clamp the rod windmills -- a clean 29 deg beat whose centre drifts to +32 deg, which on amplitude alone reads as a beat. (before the run)
