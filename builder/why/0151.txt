time   2026-09-22 09:43:31
spec   cil_s1_onerod
camera azim=35.0 elev=12.0 zoom=1.3
why    STEP 1, ACTUALLY centred this time, and the reason it was not is worth recording. boundary: free draws the scene box at [-world/2, +world/2] -- centred on the ORIGIN -- not at [0, world]. The rod was based at (0.5, 0.5, 0.3), which in that box is the far +x+y+z CORNER, with its tip poking out of the top. And the two render paths disagreed about it: the -o plot replay does NOT re-centre, so a direct render put the rod neatly inside the box while the watcher showed it hanging off the edge, and I trusted the direct render twice. The rod is now based at (0, 0, -0.2) and spans z -0.2 to +0.2, centred on the origin, which is where a free scene's centre is. Also THREE operators where there were six: rod_stretch+rod_bend -> rod_elastic, rod_pin+rod_base_moment -> rod_base, and rod_drag deleted because motion_ops.drag only lacked a tangent. Behaviour-identical throughout: base 60.297 deg, tip 31.752, tip/base 0.53, period 0.9526 s against a commanded 1.000, sperm number 1.49. (before the run)
