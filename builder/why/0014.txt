time   2026-09-19 10:14:09
spec   plat_r1_00_outline
camera azim=20.0 elev=8.0 zoom=1.45
why    R1.00 the body outline. All 4,117 somata are seeded at their measured positions (neural_seed against the frozen region, which CHECKS the 195.9 um cube against the manifest rather than assuming it) but none is drawn: this rung is the animal's own segmented surface and nothing else, so every layer after it can be seen sitting INSIDE the body rather than floating.
