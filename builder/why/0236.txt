time   2026-09-22 13:48:35
spec   cil_s9_slowbeat
camera azim=35.0 elev=12.0 zoom=1.3
why    BACK AND FORTH AT 1 Hz, TWO CYCLES. 0228 showed the tip go one way and drift back, and the measurement says why: its period was 0.680 s against a commanded 0.100, so the tip was not following the 10 Hz drive. Making the rod x100 denser is what let the drive reach it at all -- through the grid's mass-weighted average, where a water-density rod is only 0.53% of the mass it is averaged with, so 99.5% of each substep's drive is discarded. That took the tip from 0.00 to 4.79 deg with nothing else changed. But density raises inertia, and a heavy rod in a viscous fluid low-passes, so it cannot follow 10 Hz: hence 1 Hz. The 800-frame version of this died with cudaErrorStreamCaptureInvalidated, a blow-up inside the captured graph and the same error the density x1000 rung gave -- the x100 rod was stable at 10 Hz over 200 frames, so it destabilises with TIME rather than with beat rate. Two cycles shows the stroke both ways and is short enough to say whether the instability is slow. The density stays a DIAGNOSTIC: a cilium is not denser than water, and the real fix is a drag-based momentum exchange instead of mpm_gather's velocity replacement. (after the run)
