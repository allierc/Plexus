time   2026-09-22 12:09:30
kind   measurement (tip angle vs Reynolds)
why    MEASUREMENT, not a run: the tip angle across the four runs above, and the test of whether the rod lying down is a Reynolds effect. IT IS NOT. 0164 (water, moment 1, Re 7.9e3) sits upright at +0.3 deg. 0182 is the ONE-VARIABLE test -- the identical spec with only the water's kinematic viscosity changed, nu 1e-6 to 0.9, dropping Re by five orders to about 1e-2 -- and if the steady-streaming diagnosis were right its mean would go to zero. Read the right-hand panel for what it actually did. 0180 changed the moment as well (1.0 to 1.6e5) and is shown only to mark that it confounds two variables: that moment drives at M/|e| = 9.2e6 against the clamp's restoring 1.4e3, 6,600x its authority, so the rod is bent over by brute force. Every explanation I have given this session that rests on Reynolds has to survive this panel.
