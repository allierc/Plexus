time   2026-09-22 11:20:19
spec   cil_s3_onlywater
camera azim=35.0 elev=12.0 zoom=1.3
movie  mp4/0164.mp4  (46.5 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s3_onlywater/movie.mp4)
why    THE CONTROLLED COMPARISON against 0152: the SAME ROD, with water added and nothing else changed. Every rod parameter is copied verbatim from cil_s1_onerod -- 24 nodes, length 0.4, 1 Hz, moment 1.0, clamp 40, k_bend 5e4, k_stretch 1e5, zeta_stretch 0.3, zeta_bend 0.05, same direction, same beat axis -- and the camera is 0152's (azim 35, elev 12, zoom 1.3). 0160 was NOT this comparison and I should not have let it stand as one: it changed four of those settings (omega 1 to 2 Hz, moment 1 to 800, clamp 40 to 15, k_bend 5e4 to 5e5) because a moment of 1 in water gave a 1.0 degree base swing, so it could not isolate what water alone does. WHAT ADDING WATER NECESSARILY DRAGS WITH IT, so 'only water' is not overclaimed: the stand-in drag goes (keeping it beside real water would count the fluid twice, and replacing it IS the experiment); the rod becomes entity mpm_particle so scatter/gather can carry momentum, with Young's modulus 1.0 against the water's bulk modulus of 1e5 so rod_elastic still supplies all its stiffness; boundary free becomes wall because MPM water needs a container, which moves the drawn box from [-0.5,+0.5] to [0,1] and the base from (0,0,-0.2) to (0.5,0.5,0.3), the same place relative to the box centre; and the schedule gains a 5e-4 substep, a FINER step than 152's flat 1e-3. (after the run)
