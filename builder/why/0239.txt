time   2026-09-22 14:01:50
spec   cil_s10_drag
camera azim=35.0 elev=12.0 zoom=1.3
why    THE ROD LEAVES THE MPM TRANSFER AND COUPLES BY DRAG INSTEAD. Every water run before this made the rod's nodes entity: mpm_particle and let mpm_scatter/mpm_gather carry the momentum, and for a SLENDER body that cannot work. The measurement is stark: a rod node weighs 7.3e-03 while the 3x3x3 stencil it scatters over holds 1.39 of water, so the node is 0.53% of the mass its own velocity is averaged with -- and mpm_gather REPLACES a particle's velocity with that average, so over 99% of each substep's drive is discarded before it returns. Evidence: the base swing fell from 60.3 deg in air to 1.0 in water; a clamp of 40 rad/s let the base flop to +107 deg while 8e4 gave exactly 0.00, with the operator's own arithmetic verified by a unit test; density x100 moved the tip from 0.00 to 4.79 deg, which IDENTIFIED the cause, and x1000 diverged. So the rod is no longer in the transfer. It keeps its own pos and vel, integrated by the engine, and exchanges momentum through  extended with  -- the drag is measured against the fluid's B-spline-interpolated velocity rather than against rest -- and , which scatters the equal and opposite onto the same grid nodes with the same weights. A coupling, not a sink: the rod feels the water and the water feels the rod. It runs after mpm_grid_update and before mpm_gather, the only place it can, since the grid velocity is undefined until the update and the reaction must be there before the water gathers. AND THE ROD GOES BACK TO 152's PARAMETERS -- density 1100, moment 1.0, clamp 40, k_bend 5e4, k_stretch 1e5 -- because those beat 31.8 deg at Sp 1.49, and the whole claim is that the coupling and not the parameters was what stopped them. (before the run)
