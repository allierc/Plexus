time   2026-09-22 13:39:25
spec   cil_s9_slowbeat
camera azim=35.0 elev=12.0 zoom=1.3
why    BACK AND FORTH, NOT JUST FORTH. 0228 showed the tip go one way and drift back, and the measurement says why: its period came out at 0.680 s against a commanded 0.100, so the tip was not following the 10 Hz drive at all. Making the rod a hundred times denser is what let the drive reach it through the grid's mass-weighted average -- that took the tip from 0.00 to 4.79 deg with nothing else changed -- but it also raised the rod's inertia, and a heavy rod in a viscous fluid low-passes: it cannot follow 10 Hz. So the beat returns to 1 Hz and the run to 800 frames, which is four full cycles sampled 200 times, 50 frames a cycle. The sperm number falls with omega^(1/4) as well, 7.3 back to 4.1, which is a real sperm's. The density remains a DIAGNOSTIC and not a proposal: a cilium is not denser than water, and the real fix is a drag-based momentum exchange instead of mpm_gather's velocity replacement, after which the density goes back. (before the run)
