time   2026-09-22 13:18:03
spec   cil_s7_beat
camera azim=35.0 elev=12.0 zoom=1.3
movie  mp4/0221.mp4  (22.6 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s7_beat/movie.mp4)
why    ITERATION on 0219: the clamp and the pin now scale WITH the drive. 0219 had the right viscosity, the right stiffness and the right stretch spring -- worst segment strain 0.27%, so nothing was pulling apart -- but its base flopped over to +107 deg and the rod curled to 81% end-to-end. The reason is arithmetic I should have done before running it: the moment drives the base joint at M/|e|^2 = 1.1e9 rad/s^2, while a clamp of 40 rad/s restores only clamp^2 * theta = 2.8e2 at 10 degrees. Four MILLION times weaker. Raising the drive without raising what holds the base just flips the base. The clamp that holds it at 10 degrees is sqrt(M/(|e|^2 * 0.175)) = 7.9e4 rad/s, needing dt < 2/clamp = 2.5e-5 against our substep of 8.3e-6, so it fits; the position pin goes 600 -> 1e5 because the base node had slid 3.2e-2 off its anchor. Everything else is unchanged and measured: nu 1.6 (100x the bench's measured nu_eff of 0.016), moment 3.33e5 (the measured linear slope extrapolated to a 10 deg tip), k_stretch 3.67e10 (3% strain at that moment), k_bend 1e7 (Sp 4.1), 600 substeps a frame from 2/sqrt(k_stretch). Re 0.11 and Sp 4.1 together. (after the run)
