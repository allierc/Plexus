time   2026-09-22 09:31:51
spec   cil_s1_onerod
camera azim=35.0 elev=12.0 zoom=0.35
why    STEP 1, centred in the SCENE and drawn as a filament, with three operators where there were six. The rod sits at x=y=0.5 spanning z 0.3 to 0.7 -- dead centre of the unit box; the shots that looked off-centre were the CAMERA, which auto-frames on the subject and at 24 nodes zooms until the box leaves the picture, so this one is zoomed out to show the scene the rod is centred in. rod_stretch + rod_bend -> rod_elastic (one material). rod_pin + rod_base_moment -> rod_base (the same moment across the same joint: one takes a restoring law, the other a command). rod_drag deleted, because motion_ops.drag already existed and only lacked a tangent, so it gained 'along: chain'. The drag stays OUT of rod_elastic on purpose -- everything in rod_elastic is an equal-and-opposite pair or a (-1,+2,-1) triple, while drag is a sink into a fluid that is not represented, and merging them would hide a momentum leak inside an operator whose name promises there is none. Behaviour-identical to the six-operator version: base 60.297 deg, tip 31.753, tip/base 0.53, period 0.9526 s against a commanded 1.000, sperm number 1.49. (before the run)
