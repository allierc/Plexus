time   2026-09-22 11:02:04
spec   cil_s2_water
camera azim=35.0 elev=12.0 zoom=0.9
why    STEP 2 with a 51.4 deg tip sweep -- LARGER than the 31.8 deg the rod managed in air in 0152 -- at 2 Hz, eight cycles over the run. The lever was the CLAMP, which holds the first segment's DIRECTION and so fights the base swing head on; at 40 rad/s against a 12.57 rad/s beat it was only 3x the drive, and at 15 the rod is free to sweep. Driving harder does NOT help past a point: moment 2500 gives a 7.6 deg tip against 800's 51.4, because mpm_gather caps velocity at 0.4*dx/dt_sub = 12.5 world units per second and anything past that is clipped. WHAT WATER CHANGES ABOUT THE STROKE, and it is not a setting: in air the whole rod swung from its base (60.3 deg base, 31.8 tip, ratio 0.53, tip LAGGING); in water the base barely moves at 5.2 deg and the far part sweeps instead, ratio 9.9 -- a whole-rod sweep has to push the entire column of fluid, so the drive goes into bending rather than rotation. Same rod, same operators; the fluid decides which mode it can afford. Colour is water SPEED through inferno, black still, range [0, 0.04] world/s from the measured 95th percentile of 2.04 um/s. Re is 677 against a real larva's 0.16, so this is an inertial paddle, not a Stokes cilium. (before the run)
