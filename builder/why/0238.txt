time   2026-09-22 13:56:31
spec   cil_s9_slowbeat
camera azim=35.0 elev=12.0 zoom=1.3
movie  mp4/0238.mp4  (16.2 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s9_slowbeat/movie.mp4)
why    BACK AND FORTH AT 1 Hz, TWO CYCLES, retried on cuda:1. The identical spec failed at FRAME 0 on cuda:0 with cudaErrorStreamCaptureInvalidated, and a full diff against the working cil_s8_dense shows only three differences -- the name, 200 frames against 400, and omega 62.8 against 6.28. None of those can fail at frame 0, where the drive is sin(0) = 0 either way, so the suspect is the DEVICE rather than the spec: cuda:0 had another process resident and had just been used by a run that died the same way. This run is the control for that. (after the run)
