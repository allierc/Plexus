time   2026-09-22 09:04:42
spec   cil_s1_onerod
camera azim=90.0 elev=0.0 zoom=1.0
movie  mp4/0146.mp4  (8.3 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s1_onerod/movie.mp4)
why    STEP 1, centred and drawn as a filament. The rod's base moves to z = 0.3 so the 0.4-long rod spans 0.3 to 0.7 and sits in the middle of the box, and plotting.chain draws its 24 nodes as a connected TUBE. The renderer previously had no way to draw a filament at all: _edge_actor works from a half-edge MESH table (E_srce/E_trgt/E_face), which the vertex model has and a particle chain does not, so a correctly bending rod rendered as eighteen loose dots. The connectivity needs no storage -- consecutive rows ARE the filament, by the layout rod_seed writes. The blue was a separate bug: plotting.colors keys on TYPE names and this set has none, so the entry was ignored and the default applied. (after the run)
