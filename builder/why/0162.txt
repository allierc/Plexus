time   2026-09-22 11:09:21
spec   cil_s2_water
camera azim=35.0 elev=12.0 zoom=1.3
movie  mp4/0162.mp4  (53.5 MB, copied from /groups/saalfeld/home/allierc/GraphData/graphs_data/studio/cil_s2_water/movie.mp4)
why    STEP 2 framed exactly as 0152, so the two can be compared side by side. The ORIENTATION was never different: both rods point along +z (direction [0,0,1]) and sweep in the same plane (beat_axis [0,1,0], up_axis 2), and both shots use azim 35 elev 12. What differed was the ZOOM -- 1.3 for 0152 against 0.9 here -- and the box, which moves from [-0.5,+0.5] for a free scene to [0,1] for a walled one, so bio_view frames it differently. This is 0160's physics at 0152's framing. The BEAT genuinely differs, and four settings plus one physical fact separate them: 1 Hz -> 2 Hz, moment 1 -> 800, clamp 40 -> 15, k_bend 5e4 -> 5e5. The physical fact is that the fluid decides which mode the rod can afford: in air the WHOLE ROD swings from its base, 60.3 deg base and 31.8 tip, ratio 0.53 with the tip LAGGING, which is the elastohydrodynamic stroke; in water a whole-rod sweep would have to push the entire column of fluid, so the drive goes into bending instead -- the base sits at 5.2 deg and the far part sweeps at 51.4, ratio 9.9. Lowering k_bend to restore the lag does not recover 0152's shape, it makes the tip dead (ratio 0.11 at 5e4), because the fluid is what set the mode. (after the run)
