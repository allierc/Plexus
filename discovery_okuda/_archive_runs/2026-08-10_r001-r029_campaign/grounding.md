# Grounding — round r028

**No advance — the same FORCED star, now for a FOURTH round.** `r028_00_ctrl` == `r028_06` are bit-identical to the flagged parent (`protr_peak` **1.595**, `gyr_prolate` 3.008, `mech_p_ratio` **1.69**), and r026's no-force ablation still stands: remove `extrude` and the finger collapses to ~1.17. The eye reads "genuine grown protrusions," but `mech_p_ratio` 1.69–1.86 plus that ablation say pushed, not grown — the eye is not the independent witness the campaign requires against a forcing operator. `morphology='sphere'` is broken all round; read `protr_peak` + eye, not the label.

**Closest to Fig. 5a (~10 thin tubes):** `r028_03` — ~10 red-tipped radial fingers, `protr_peak` 1.366, `n_tubes` 10, `mech_p_ratio` **1.796** (forced). Multi-fingered and forced, not one grown constant-width tube. `r028_05` is a knobbly ball (eye: zero tubes) despite the `tube` label.

**The gap is unchanged:** a GROWN thin tube ≥1.3 is still unproduced; grown-alone caps ~1.17. So the deficit is the mechanism, not the amplitude.

**Growth overshoots:** 2000 → 7559 cells (3.8×, `r028_03`) vs Okuda's 2032 → 3572 (1.76×).

**Untested, and the move:** Okuda's tubes come from a mechanics leg (line tension / bending), NOT radial push — four rounds of `extrude` cannot answer it. `r028_08–12` empty (5 of 16 slots lost again); `r028_13`==`14`==`15` a duplicate triple.
