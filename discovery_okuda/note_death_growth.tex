\documentclass[10pt,a4paper]{article}
\usepackage[margin=1.8cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}
\usepackage{graphicx}
\usepackage{float}
\usepackage[T1]{fontenc}
\usepackage{microtype}
\usepackage[colorlinks,linkcolor=black,citecolor=black,urlcolor=blue]{hyperref}
\usepackage[dvipsnames]{xcolor}
\usepackage{titlesec}
\definecolor{passgreen}{rgb}{0.05,0.50,0.20}
\definecolor{failred}{rgb}{0.72,0.13,0.13}
\newcommand{\PASS}[1]{\textcolor{passgreen}{\textbf{#1}}}
\newcommand{\FAIL}[1]{\textcolor{failred}{\textbf{#1}}}
\newcommand{\PEND}[1]{\textcolor{gray}{#1}}
\titlespacing*{\section}{0pt}{6pt}{2pt}
\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\op}[1]{\texttt{#1}}
\title{\vspace{-1.4cm}\textbf{Two reaction--diffusion species driving growth and death:\\
\normalsize is the mechanism sound as biology, as code, and as Plexus2?}}
\author{}
\date{\vspace{-0.9cm}\small \texttt{tyssue\_rd\_ops} $\cdot$ \texttt{tyssue\_ops3d} $\cdot$
\texttt{make\_two\_species.py} $\cdot$ \texttt{make\_sculpt.py} $\cdot$
\texttt{log/okuda/\{ts,tsd,sc\}\_*} (24 preliminary runs) $\cdot$ 11 August 2026}
\setlength{\parindent}{0pt}\setlength{\parskip}{0.55em}
\begin{document}\maketitle\vspace{-1.0cm}

\textbf{Scope.} This note is about one addition to the framework: a second reaction--diffusion
species, and the two operators that consume it --- growth inhibition and cell death. It asks whether
that addition is \emph{sound}, on three axes: as biology, as code, and as Plexus2. \textbf{It does
not ask whether the mechanism achieves the campaign's goal.} Twenty-four preliminary runs exist in
\code{log/okuda} and they raised a specific set of defects; \S6 is that list, structured, with each
one's status. A mechanism whose implementation is unverified cannot be compared with an observation
at all, so the goal question is deferred until this note's gates close.

\section*{1.\; The mechanism}

\textbf{One field cannot pattern two processes.} With a single Gray--Scott pair every operator reads
the same activator --- growth is gated on it, death is gated on it, the purse-string selects on it ---
so the tissue carries exactly one map. ``Where it grows'' and ``where it dies'' are then the same
region or exact complements of it, by construction, and no setting of any parameter separates them.
This is a property of the representation.

Two species is two maps. Biologically this is unremarkable and well attested: a tissue carries many
morphogens at once, on different length scales, read by different targets. Two are enough to state
the smallest interesting case --- one field says \emph{grow here}, another says \emph{stop} or
\emph{die here} --- and that is the case this note verifies.

\textbf{The two consumers are drawn from different literatures.} Growth inhibition is lateral
inhibition in the Gierer--Meinhardt sense: a diffusible signal that suppresses the response of the
cells around a source. Patterned death is Monier \emph{et al.} (2015), where apoptotic force drives
epithelial folding in \emph{Drosophila} --- and it is the only \emph{inward} deformation available
in this vocabulary, every other operator pushing the sheet outward.

\textbf{The arithmetic that fixes the representation.} A dying cell leaves an epithelium by losing
neighbours until it is a triangle; a triangle collapses to a point as a T2 transition, $V-2$, $E-3$,
$F-1$, so $\chi = V-E+F$ is unchanged and the sheet stays closed at genus~0. Collapsing a face with
$k>3$ sides would merge $k$ vertices into one and leave a vertex of degree $k$, \textbf{which a
trivalent sheet cannot represent}. So death cannot be one operator: it is mark, then shed, then
extrude, and the shedding belongs to \op{reconnect\_t1\_3d}. That is not a hidden coupling; it is
the mechanism. A cell leaves an epithelium by losing neighbours.

Two ratios fix the proportions. The species' wavelengths must differ or the second map is
redundant: measured on a static sphere, species~A at $\chi=1.3$ holds $2$ spots at a spacing of
$6.99$ cell diameters and species~B at $\chi=0.4$ holds $15$ at $3.18$ --- a factor of $2.2$, with
\textbf{A} the coarse map. $\chi$ sets this, not the diffusivity pair: at $\chi=1.3$ with doubled
diffusivities the field does not pattern at all. And the clearing time of a dying cell is fixed by
two numbers and nothing else (Eq.~\eqref{eq:clear}).

\section*{2.\; Equations and symbols}

\textbf{Two independent systems in one buffer.} \code{chem} is width~4; species~A occupies columns
$(0,1)$ and species~B columns $(2,3)$. Each has its own seeder, diffusion and reaction, and each
operator declares which pair it owns through \code{chan}:
%
\begin{equation}
\frac{\partial a_X}{\partial t}=D^X_a\nabla^2 a_X + f(a_X,u_X),\qquad
\frac{\partial u_X}{\partial t}=D^X_u\nabla^2 u_X + g(a_X,u_X),\qquad X\in\{A,B\},
\label{eq:two}
\end{equation}
%
with $f,g$ Gray--Scott. The two systems are coupled \emph{only} through the operators that read
them, never through the buffer --- which is a property that must be gated (G7), not assumed.

\textbf{Growth, and the zero it did not have.} The standing law is purely activating,
%
\begin{equation}
\dot v_{\rm eq}=\texttt{rate}\cdot\bigl(\rho+\mathrm{hill}(a_A)\bigr),\qquad
\mathrm{hill}(x)=\frac{x^{n}}{x_{\rm sw}^{n}+x^{n}}\in[0,1],
\label{eq:growth}
\end{equation}
%
so the slowest a cell can grow is $\texttt{rate}\cdot\rho$, and $\rho>0$ is required by premise~P1
(a tissue that adds no material is not growing). The inhibitor supplies the missing zero,
multiplicatively:
%
\begin{equation}
\dot v_{\rm eq}=\texttt{rate}\cdot\bigl(\rho+\mathrm{hill}(a_A)\bigr)\cdot
\bigl(1-\mathrm{hill}_{\rm inh}(\hat a_B)\bigr),\qquad \hat a_B=a_B/\max_f a_B .
\label{eq:inhib}
\end{equation}
%
$\hat a_B$ is normalised to the inhibitor's own maximum for the same reason $a_{\rm sw}$ is a
fraction: a threshold stated absolutely, against a field whose scale the chemistry sets, is either
always on or always off.

\textbf{Death, as three steps and one closed-form time.}
%
\begin{equation}
V^0_f \leftarrow (1-s)V^0_f,\qquad \text{extrude when } V^0_f<c\,v_{\rm ref},\qquad
\boxed{\;\tau_{\rm clear}=\frac{\ln c}{\ln(1-s)}\ \text{ticks}\;}
\label{eq:clear}
\end{equation}

\textbf{Volume is conserved into the survivors, and the conservation is bounded.} When $f$ dies its
remaining material is shared over its live ring neighbours, and the increment is capped so the
recipient stays inside the integrator's basin:
%
\begin{equation}
c_g \leftarrow c_g + \min\!\Bigl(\frac{\text{amt}/|\mathcal L|}{V^0_g},\;
c_{\max}-c_g\Bigr),\qquad
\mathcal L=\{g\in\mathcal N(f): V^0_g\ge c\,v_{\rm ref}\},
\label{eq:conserve}
\end{equation}
%
with $c_{\max}$ the field's own running maximum. Material that will not fit is \emph{dropped and
counted} in \code{apop\_spill}, so the conservation law reports its own error rather than
violating itself silently. Both restrictions are gated (G4, G5).

\textbf{A death RATE, not a death set}: with $N$ live cells, $|\{\text{marked}\}|\le\phi N$, and when
the cap binds the worst candidates are taken by the mode's own severity. Geometric modes are exempt,
because a set chosen once has no flux.

\smallskip
{\small
\begin{tabular}{@{}llp{8.4cm}@{}}\toprule
$a_X,u_X$ & activator, substrate of species $X$ & \emph{per cell}, INTENSIVE. A concentration, so it
  is meaningful to compare between cells of different size\\
\code{chan} & species selector & which \code{chem} pair an operator owns. Default 0, so every
  pre-existing spec is unchanged\\
$D^X_a,D^X_u$ & diffusivities & the RATIO sets the Turing wavelength; the pair sets the speed\\
$\rho$ & growth baseline & tip:body is $(\rho+1):\rho$. $\rho=0$ is an infinite ratio, which P1 refuses\\
$x_{\rm sw}$, $n$ & gate threshold, sharpness & a FRACTION of that field's own maximum, never absolute\\
$s$, $c$ & \code{shrink\_rate}, \code{critical\_frac} & per tick, and a fraction OF $v_{\rm ref}$\\
$v_{\rm ref}$ & reference volume & the SEED-TIME median. Never recomputed: recomputing it after
  deaths moves every threshold in the model\\
$\tau_{\rm clear}$ & clearing time & INTENSIVE: a property of $(s,c)$ alone, not of the tissue\\
$\phi$ & \code{max\_mark\_frac} & fraction of LIVE cells under sentence at once --- bounds a FLUX\\
$n_{\rm apop}$ & deaths & EXTENSIVE: scales with the tissue, so never comparable between runs of
  different size without dividing by $N$\\
\code{apop\_spill} & unplaced material & EXTENSIVE. The conservation law's own error term\\
\bottomrule
\end{tabular}}

\section*{3.\; The Plexus2 container}

No new set. What is new is a \emph{family} the vesicle lacked --- \emph{Die} --- and a second species
inside one state buffer. Three claims about the container have to hold, and each is a gate:

\begin{enumerate}\itemsep0pt
\item \textbf{Two instances of one operator are ADDITIVE, not overwriting.} \op{cell\_react}
  returns a delta over the whole \code{chem} tensor and writes zeros outside the pair its
  \code{chan} names. Without that, a second instance overwrites the first and ``two species'' is a
  name for one. (G7)
\item \textbf{\op{apoptosis\_3d} is \code{structural}, not \code{lateral}.} It changes how many
  entities exist, so it may not be a pure state map: it renumbers faces, and every per-face array
  and every \emph{pending delta} must be permuted with it. (G3)
\item \textbf{A named population is a seed window, one level up.} \op{list}, \op{band} and \op{cone}
  choose a population ONCE. Re-evaluated every frame, a death sentence becomes a death rate --- the
  same argument the paper makes for seeding, where an initial condition re-applied every step is a
  moving boundary condition. (G6)
\end{enumerate}

\begin{center}\footnotesize
\begin{tabular}{@{}lllp{4.4cm}p{2.6cm}@{}}\toprule
\textbf{operator} & \textbf{kind / family} & \textbf{reads} & \textbf{mechanism} & \textbf{gate}\\\midrule
\op{seed\_cell\_rd} & seed / growth & --- & seeds its own columns & G7\\
\op{cell\_diffuse}  & lateral / fields & \code{chem}, adjacency & per-column diffusivity vector & G7\\
\op{cell\_react}    & lateral / fields & \code{chem} & Eq.~\eqref{eq:two}, zeros outside its pair & G7\\
\op{grow\_3d}       & structural / growth & \code{chem} & Eq.~\eqref{eq:inhib} & G8, G9\\
\op{apoptosis\_3d}  & \textbf{structural / die} & \code{chem}, mesh &
  Eqs.~\eqref{eq:clear}--\eqref{eq:conserve} & G1--G6\\
\op{reconnect\_t1\_3d} & rewire / topology & mesh & sheds neighbours --- REQUIRED & G2\\
\bottomrule
\end{tabular}
\end{center}

\textbf{Schedule.} Death runs \emph{between growth and the mechanics}: growth sets the targets, death
overrides them for the sentenced, and the relaxation sees both in one tick, so a cell extruded this
frame has its hole closed this frame. Measured, same composition with death appended after the
recorder instead: grip $0.031$ against $0.110$. There is no \code{substep\_dt} block --- this vesicle
integrates at one rate and \op{shape\_energy\_3d} carries \code{relax\_iters} internally.

\section*{4.\; The gates}

Thresholds set before the run. The tiers are the paper's: \textbf{bookkeeping} asks whether the code
does what the operator says, \textbf{closed form} whether it reproduces physics it was given, and
\textbf{measurement} whether it agrees with something observed in cells.

\smallskip
{\small
\begin{tabular}{@{}llll@{}}\toprule
& \textbf{gate} & \textbf{threshold, before} & \textbf{measured}\\\midrule
\multicolumn{4}{@{}l}{\textit{tier 1 --- bookkeeping}}\\
G1 & every selector fires on a known population and NOT on a uniform field &
     all cases & \PASS{19/19}\\
G2 & $\chi=2$ at every frame with death running & exact & \PASS{2, all frames}\\
G3 & a topology change permutes pending DELTAS, not only state &
     activator $\ge0$ & \PASS{0} (was $-1.04{\times}10^{10}$)\\
G4 & a bequest goes only to neighbours above the extrusion threshold &
     no divisor $<c\,v_{\rm ref}$ & \PASS{P4, P12 cleared}\\
G5 & a bequest leaves the recipient inside the integrator's basin &
     no NaN, activator $\ge0$ & \FAIL{RE-RUNNING} --- \S6 I2\\
G6 & a named population is chosen ONCE; only state modes re-evaluate &
     one death per named cell & \PASS{1, was 7}\\
G7 & two species never write each other's columns &
     $\{0,1\}$ and $\{2,3\}$ & \PASS{exact, non-uniform field}\\
G8 & \code{inhib\_chan} absent reproduces a pre-change run &
     metrics identical & \FAIL{RUNNING} --- \S6 I3\\
G9 & growth $\to0$ as the inhibitor saturates & $<5\%$ of uninhibited &
     \PASS{$0.978\to0.014$}\\
G10& the cap admits at most $\phi N$, worst first, and drains &
     exact & \PASS{8/100; took worst; 4 held}\\
G11& $n_{\rm apop}$ equals the cells removed & exact, death-only composition &
     \PASS{$400-N_{\rm final}$}\\
\midrule
\multicolumn{4}{@{}l}{\textit{tier 2 --- closed form}}\\
G12& a collapse is a T2: $V-2$, $E-3$, $F-1$ & exact, else refuse &
     \PASS{refuses $k>3$, re-checks closure}\\
G13& clearing time equals $\ln c/\ln(1-s)$ & within 1 tick &
     \FAIL{NOT MEASURED} --- predicted 12/37/94/189\\
G14& the throughput lever is $s$, not $\phi$ & deaths $\propto1/\tau_{\rm clear}$ &
     \PASS{$\phi{\times}50\!\to\!{\times}1.7$; $s{\times}3\!\to\!{\times}4.7$}\\
G15& a geometric chisel removes exactly its declared population & exact &
     \PASS{5/5 geometries}\\
G16& the second species' wavelength differs from the first & spot count differs &
     \FAIL{NOT MEASURED} --- \S6 I5\\
\midrule
\multicolumn{4}{@{}l}{\textit{tier 3 --- measurement}}\\
G17& apoptotic rate matches an epithelium's & $1$--$3\%$ of cells per day &
     \PEND{BLOCKED: no \code{units:}}\\
G18& clearance time matches an extrusion in vivo & $30$--$90$ min &
     \PEND{BLOCKED: no \code{units:}}\\
G19& the two species' wavelengths bracket a real morphogen pair & --- &
     \PEND{BLOCKED: no \code{units:}}\\
\bottomrule
\end{tabular}}
\smallskip

\textbf{Eleven bookkeeping, five closed form, three measurement --- and all three of the last are
blocked} for want of a \code{units:} block, the same defect the \code{ecm} study found when
``$0.82$ grid cells'' turned out to be $15\,\mu$m. So this note can establish that the operators do
what they say and reproduce the arithmetic they were given. \textbf{It cannot yet say anything about
cells.} Declaring units is the single change that would move three gates from blocked to failable.

\section*{5.\; What the runs establish}

Twenty-four exploratory runs on \op{r001\_03} and fifteen dedicated gate runs. Three results are
properties of the operators rather than of any morphology, and are the ones this note rests on.

\textbf{Position dominates rate.} Death patterned in SPACE deforms the body at a thirtieth of the
cell cost of death patterned by rate:

\smallskip
{\small\begin{tabular}{@{}lrrrrr@{}}\toprule
run & deaths & cells & protr\_f & invag\_f & red\_vol\\\midrule
\op{sc\_crown} (a polar cap)      & \textbf{113} & 16056 & \textbf{1.267} & \textbf{0.343} & \textbf{0.814}\\
\op{sc\_waist} (an equatorial band)&  332 & 14486 & 1.174 & 0.328 & 0.807\\
\op{tsd\_max} (the highest rate)   & \textbf{3460} &  2895 & 1.140 & 0.111 & 0.957\\
control (no death)                 &    0 & 12692 & 1.095 & 0.120 & 0.959\\
\bottomrule\end{tabular}}
\smallskip

Scattered death thins a ball into a smaller ball; a chosen population is a cut.

\textbf{The throughput lever is $\tau_{\rm clear}$, not the cap.} Fifty times $\phi$ moves the death
count by $1.7\times$; tripling $s$ at fixed $\phi$ moves it by $4.7\times$. The queue does not fill,
so what binds is how fast a sentenced cell reaches the extrusion threshold (G14).

\textbf{Inhibition is monotone and is a brake.} $12692 \to 6920 \to 4859 \to 4154$ cells as
\code{inhib\_sw} tightens, with \code{protr\_final} falling $1.095 \to 1.020$. Eq.~\eqref{eq:inhib}
asserts a multiplicative shutdown and the tissue size follows it; the shape does not sharpen.

\section*{6.\; Gate outcomes and open items}

\textbf{G8 --- the engine is deterministic, and the trajectory still moved.} Two replicates of one
spec, same seed, different names: \op{g8\_rep1} and \op{g8\_rep2} agree to every recorded digit
(7099 cells, \code{protr\_peak} 1.175, \code{act\_max} 0.825). So a per-run comparison inside one
build is exact, and the seed spread is the only noise floor within it. But both differ from the
same spec's archived run (7261 cells, 1.176): a numerically equivalent refactor of
\op{cell\_diffuse}'s coefficient vector and \op{cell\_react}'s delta shifted a chaotic trajectory
by $2.2\%$ in cell count over 1800 frames. \textbf{Comparisons ACROSS that change are not exact;
comparisons within a build are.}

\textbf{G13 --- consistent, and not resolvable at this stride.} One named cell on a static sheet,
four shrink rates. The death frames order as predicted ($\sim$150, 100, 100, 75 against $87$, $68$,
$62$, $58$) and each exceeds its prediction, which is the shedding time
Eq.~\eqref{eq:clear} does not include. But \code{metrics.npz} carries 25 samples for 601 frames --- a
stride of 25 --- and the differences between rungs are of the same order. \textbf{The formula is not
contradicted and is not confirmed.} Closing it needs the frame each marked cell crosses
$c\,v_{\rm ref}$ recorded beside the frame it is extruded, which separates the two terms and does
not depend on the sampling stride at all.

\textbf{G16 --- closed, failed, and the direction is the finding.} Species B is the FINE map:
15 domains at a spacing of $3.18$ cell diameters against A's 2 at $6.99$. $\chi$ sets this, not the
diffusivity pair --- at $\chi=1.3$ with doubled diffusivities the field does not pattern at all
(\code{act\_max} $0.0$). The requirement the gate stands for, that the two maps differ, holds at
$2.2\times$.

\textbf{G5 --- closed on the run it was built for.} \op{tsd\_cap10} previously reached
\code{act\_mean\_floor} $-7.29\times10^{11}$ and went NaN at frame 1430. With the bequest bounded
per column it finishes with \code{act\_max\_final} $0.579$, \code{act\_mean\_floor} $0.0465$, no
extinction and no premise broken --- and \code{apop\_spill} $3.009$, so material WAS refused and the
bound engaged rather than passing vacuously. That also closes \textbf{I8}: the conservation law's
error term is recorded and readable.

\textbf{I4 --- closed, and it confirms the throughput law while removing the confound.} The slow
ladder with \code{inhib\_chan} absent:

\smallskip
{\small\begin{tabular}{@{}lrrrr@{}}\toprule
run & $\tau_{\rm clear}$ & deaths & cells & \code{apop\_spill}\\\midrule
\op{i4\_slow005} & 37  & 875 &  8482 & 4.14\\
\op{i4\_slow002} & 94  & 729 & 10534 & 2.45\\
\op{i4\_slow001} & 189 & 199 & 11553 & 0.29\\
\bottomrule\end{tabular}}
\smallskip

Deaths fall and tissue rises monotonically with the clearing time, which is Eq.~\eqref{eq:clear}
acting as the throughput lever (G14) measured directly. And the confounded series' small tissue
($3525$--$4353$ cells) was the inhibitor, not the death speed: at the same shrink rates without it
the tissue holds $8482$--$11553$.

\begin{center}\small
\begin{tabular}{@{}llp{7.4cm}l@{}}\toprule
& \textbf{item} & \textbf{what closing it requires} & \textbf{state}\\\midrule
G5  & the conservation bound holds &
  \PASS{closed on \op{tsd\_cap10}}: finite activator, no premise broken, \code{apop\_spill}
  $3.009$. \op{tsd\_cap25} confirms it at the higher cap. & \PEND{cap25 running}\\
G8  & runs are reproducible &
  \PASS{deterministic within a build}; \FAIL{not across the channel refactor}. Any comparison
  spanning it must be re-measured. & \FAIL{qualified}\\
G13 & $\tau_{\rm clear}=\ln c/\ln(1-s)$ &
  a per-cell record of the crit-crossing frame, independent of the sampling stride. & \FAIL{open}\\
G16 & the two maps differ &
  \PASS{closed}: $2.2\times$ in spacing, with A coarse and B fine. & \PASS{done}\\
G16c& the pair keeps both wavelengths &
  \op{g16c\_pair} re-measured. The per-species pass was first added to \code{measure\_1frame},
  which \code{run\_one} does not call --- it carries its own per-frame loop --- so the instrument
  is now in that loop and the frame tuple carries channel~2. & \PEND{running}\\
I4  & the slow ladder, unconfounded &
  \PASS{closed}: deaths $875\to729\to199$ and cells $8482\to11553$ as $\tau_{\rm clear}$ rises
  $37\to189$. & \PASS{done}\\
I8  & \code{apop\_spill} is readable &
  \PASS{closed}: $0.29$--$4.14$ across the I4 ladder, rising with the death count, so the term
  tracks the refusals rather than sitting at zero. & \PASS{done}\\
G17--19 & any measurement-tier gate & a \code{units:} block. & \PEND{blocked}\\
\bottomrule
\end{tabular}
\end{center}

\textbf{The instrument for the second species now exists.} \code{b\_act\_max}, \code{b\_act\_cv},
\code{b\_n\_spots}, \code{b\_spot\_spacing\_cells} and \code{b\_spot\_frac} are the channel-2 twins
of the metrics that read channel 0, each declaring \code{requires} so they are UNDEFINED --- not
zero --- when the second species is extinct. They are registered as diagnostics rather than
admitted: a metric becomes admissible when a prediction may rest on it, and that requires it to
reach a real run's summary, which no two-species run has done since they were added.

\section*{7.\; What this licenses, and what it does not}

\textbf{Licensed.} Two RD species coexist in one buffer without leaking (G7), which is the structural
claim the whole addition rests on. The Die family is sound as bookkeeping: selectors fire where they
should and nowhere else (G1), the sheet stays closed (G2), a topology change carries its pending
deltas (G3), the count is exact (G11), the rate is a rate (G10), and a named population is a seed
window rather than a per-frame rule (G6). Growth can be switched off (G9), and the collapse is a
genuine T2 (G12).

\textbf{Not licensed.} \textbf{G5} is the one that matters most: until it closes, the operator can
drive the chemistry out of its basin and a run's numbers are then about an integrator.
\textbf{G13} means the clearing time --- the quantity every death experiment is denominated in ---
is asserted and not measured. \textbf{No gate is a measurement} (G17--G19 blocked for want of a
\code{units:} block), so nothing here has been compared with a cell.

\textbf{The mechanism should not enter the loop until G5, G8 and G13 close.} G5 because a mechanism
that can NaN the chemistry will produce runs the campaign records as evidence; G8 because an
operator that changes runs which do not use it corrupts every comparison in the ledger; G13 because
the clearing time is the quantity every death experiment is denominated in. G16 and I4/I5 are about
the experiments, not the operators, and can close afterwards.

\section*{8.\; References}

{\small
\begin{thebibliography}{9}\setlength{\itemsep}{0pt}
\bibitem{monier2015} Monier, B., Gettings, M., Gay, G., Mangeat, T., Schott, S., Guarner, A.,
Suzanne, M. (2015) Apico-basal forces exerted by apoptotic cells drive epithelium folding.
\emph{Nature} \textbf{518}:245--248.
\bibitem{gm1972} Gierer, A., Meinhardt, H. (1972) A theory of biological pattern formation.
\emph{Kybernetik} \textbf{12}:30--39. --- lateral inhibition, of which Eq.~\eqref{eq:inhib} is a
coarse form.
\bibitem{gray1984} Gray, P., Scott, S. K. (1984) Autocatalytic reactions in the isothermal,
continuous stirred tank reactor. \emph{Chem. Eng. Sci.} \textbf{39}:1087--1097. --- both species.
\bibitem{okuda2013} Okuda, S., Inoue, Y., Eiraku, M., Sasai, Y., Adachi, T. (2013) Reversible
network reconnection model for simulating large deformation in dynamic tissue morphogenesis.
\emph{Biomech. Model. Mechanobiol.} \textbf{12}(4):627--644. --- the T1/T2 machinery of \S1.
\bibitem{turing1952} Turing, A. M. (1952) The chemical basis of morphogenesis.
\emph{Phil. Trans. R. Soc. B} \textbf{237}:37--72. --- why two species with different
diffusivities is the minimal statement.
\end{thebibliography}}

\end{document}
