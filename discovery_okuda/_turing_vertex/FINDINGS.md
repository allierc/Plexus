# Turing x vertex — what the loop found in the settings

_Generated by `tv_report.py` from `_turing_vertex/*/diag.json`. Every prediction was
recorded in `PREDICTIONS.md` before its run and is scored here by `predict.score`._

**32 runs · surprise rate 0.36**

## The headline

In the archived minisite recipe the Gray-Scott activator **decays to nothing**, and two
further settings would keep the chemistry decorative even if it did not. Three layers,
and they have to be peeled in order:

| # | setting | what it does | measured |
|---|---|---|---|
| 1 | `conserve_amount = True` | divides `cell.chem` by the volume growth ratio every tick | activator 3.3e-2 → 4.6e-11 → **2.0e-22** at frames 60/200/400 |
| 2 | `rho > 0` | selects the capped branch: every cell pinned to the same target volume | shape stays spherical (`protr` 1.03–1.06) however strong the pattern |
| 3 | `a_sw = 50` | Hill switch far above the activator's ~0.43 peak | `hillv ~ 4e-9`; no coupling |

Layer 1 dominates: while the field is extinct, layers 2 and 3 are unobservable. Waves A
and B swept `a_sw` and `rho` against a dead field — **ten runs, every one `cells_end = 1778`** — and taught nothing about either lever.

## The lever map (live field, `conserve_amount = 0`)

| run | rho | a_sw | protr | corr(act,radius) | hollow_frac | mesh |
|---|---|---|---|---|---|---|
| `waveD_0_50` | 1.0 | 50.0 | 1.051 | +0.109 | 0.000 | intact |
| `waveD_1_0p6` | 1.0 | 0.6 | 1.042 | +0.188 | 0.000 | intact |
| `waveD_2_0p4` | 1.0 | 0.4 | 1.044 | +0.197 | 0.000 | intact |
| `waveD_3_0p25` | 1.0 | 0.25 | 1.036 | +0.043 | 0.000 | intact |
| `waveD_4_0p1` | 1.0 | 0.1 | 1.041 | +0.097 | 0.000 | intact |
| `waveE_0_1` | 1.0 | 0.3 | 1.033 | +0.292 | 0.000 | intact |
| `waveE_1_0p5` | 0.5 | 0.3 | 1.049 | +0.263 | 0.006 | intact |
| `waveE_2_0p2` | 0.2 | 0.3 | 1.060 | +0.283 | 0.008 | intact |
| `waveE_3_0p05` | 0.05 | 0.3 | 1.173 | +0.627 | 0.197 | straining |
| `waveE_4_0` | 0.0 | 0.3 | 32.727 | +0.530 | 0.839 | DESTROYED |

**`rho` is the shaping lever; `a_sw` is not.** At `rho = 1.0` the pattern is vivid and
the sphere is perfect at every `a_sw` from 50 down to 0.1 (`protr` 1.036–1.051,
`hollow` 0.000). Lowering `rho` toward the uncapped branch is what lets the shell
follow the bands.

**But the coupling and the mesh fail together — there is no clean window:**

```
rho    1.0     0.5     0.2     0.05      0.0
corr  +0.29   +0.26   +0.28   +0.63    +0.53
protr  1.03    1.05    1.06    1.17     32.7
hollow 0.000   0.006   0.008   0.197    0.839
       <--------- intact --------><strain><-- destroyed
```

`rho = 0.05` is the best coupling seen anywhere in this project (**corr +0.63**, against
0.42 for the previous campaign's best on the SPV backend) and the strip shows real
clefting along the activator bands — but 20% of faces are already folded, and the
cross-section tears by frame 400. At `rho = 0` the monolayer is destroyed outright
(84% folded, `vol_cv` 4.9): `protr = 32.7` is a needle, not a tube.

**This is a structural-limitation result, and it is first-class.** On this backend, activator-driven shaping is only reachable in a regime where the mesh is already failing. That is consistent with the prior campaign's independent conclusion that the gap is representational — quasi-static force-balance iteration and 3D reconnection are missing, not parameters.

## Scoring

| run | intent | predicted | observed protr | outcome | |
|---|---|---|---|---|---|
| `waveA_0_50` | control | `protr 1.0-1.15` | 1.051 | confirmed |  |
| `waveA_1_0p6` | confirmatory | `protr 1.0-1.15` | 1.046 | confirmed |  |
| `waveA_2_0p45` | confirmatory | `protr 1.0-1.15` | 1.040 | confirmed |  |
| `waveA_3_0p3` | confirmatory | `protr 1.0-1.15` | 1.041 | confirmed |  |
| `waveA_4_0p15` | confirmatory | `protr 1.0-1.15` | 1.051 | confirmed |  |
| `waveB_0_1` | control | `protr 1.0-1.15` | 1.041 | confirmed |  |
| `waveB_1_0p5` | confirmatory | `protr 1.0-1.15` | 1.048 | confirmed |  |
| `waveB_2_0p2` | confirmatory | `protr 1.0-1.15` | 1.064 | confirmed |  |
| `waveB_3_0p05` | adversarial | `protr 1.0-1.15` | 1.084 | confirmed | 🔥 **surprise** |
| `waveB_4_0` | confirmatory | `protr >= 1.3` | 1.034 | refuted | 🔥 **surprise** |
| `waveC_0_1` | control | `protr 1.0-1.15` | 1.041 | confirmed |  |
| `waveC_1_0` | adversarial | `protr 1.0-1.15` | 1.033 | confirmed | 🔥 **surprise** |
| `waveD_0_50` | control | `protr 1.0-1.10` | 1.051 | confirmed |  |
| `waveD_1_0p6` | confirmatory | `protr 1.0-1.15` | 1.042 | confirmed |  |
| `waveD_2_0p4` | confirmatory | `protr 1.0-1.20` | 1.044 | confirmed |  |
| `waveD_3_0p25` | confirmatory | `protr 1.0-1.25` | 1.036 | confirmed |  |
| `waveD_4_0p1` | adversarial | `protr >= 1.15` | 1.041 | refuted |  |
| `waveE_0_1` | control | `protr 1.0-1.15` | 1.033 | confirmed |  |
| `waveE_1_0p5` | confirmatory | `protr 1.0-1.20` | 1.049 | confirmed |  |
| `waveE_2_0p2` | confirmatory | `protr 1.0-1.30` | 1.060 | confirmed |  |
| `waveE_3_0p05` | confirmatory | `protr >= 1.2` | 1.173 | refuted | 🔥 **surprise** |
| `waveE_4_0` | confirmatory | `protr >= 1.3` | 32.727 | confirmed |  |
| `waveF_0_0p3` | confirmatory | `protr >= 1.10` | 1.289 | confirmed |  |
| `waveF_1_0p65` | confirmatory | `protr >= 1.10` | 1.327 | confirmed |  |
| `waveF_2_1p3` | control | `protr 1.10-1.30` | 1.173 | confirmed |  |
| `waveF_3_3` | confirmatory | `protr 1.0-1.30` | 1.430 | refuted | 🔥 **surprise** |
| `waveF_4_8` | adversarial | `protr 1.0-1.15` | 1.089 | confirmed | 🔥 **surprise** |
| `waveG_0_0p1` | confirmatory | `protr >= 1.10` | 1.096 | refuted | 🔥 **surprise** |
| `waveG_1_0p3` | confirmatory | `protr >= 1.10` | 1.085 | refuted | 🔥 **surprise** |
| `waveG_2_1` | control | `protr 1.10-1.30` | 1.173 | confirmed |  |
| `waveG_3_3` | confirmatory | `protr 1.0-1.25` | 1.270 | refuted | 🔥 **surprise** |
| `waveG_4_10` | adversarial | `protr 1.0-1.15` | 1.168 | refuted |  |

## The structural limitation (the result)

Across **32 runs spanning 5 knobs** (`conserve_amount`, `a_sw`, `rho`, `chi`, `rate`):

- **18 runs keep an intact mesh** (`hollow_frac < 0.05`). The best any of them achieves is `protr` **1.064** and `corr` **+0.292** — no shaping.
- **9 runs reach real coupling** (`corr > 0.4`). The *least* damaged of them has `hollow_frac` **0.197** — a fifth of all faces folded.
- **0 runs achieve both.**

> On this backend, **activator-driven shaping and monolayer integrity are mutually
> exclusive.** Every setting that lets the Turing pattern shape the shell also breaks
> the shell. This is a structural limitation, not a tuning failure — and it is a
> first-class result.

It is also not a new opinion: the previous Turing_vertex campaign concluded independently, on a *different* backend (spherical SPV), that *the gap is REPRESENTATIONAL not parametric*. This is the same conclusion reached by measurement on the vertex-mesh backend, with the boundary located at `hollow_frac ~ 0.197`.

Filed as **OR001** in `campaign/operator_backlog.md`: quasi-static force-balance relaxation (a residual, not a step count) plus 3D IH/HI reconnection. With an acceptance test that today's best setting fails by ~4x on both integrity metrics.

### Caveat carried, not buried

Waves F and G (the Okuda `chi`/`gamma` axes) were run at `rho = 0.05`, the only regime with coupling — where `hollow_frac` is already 0.197. **Every point in those two waves has `hollow_frac >= 0.197` and most exceed 0.46.** So their morphology numbers are measured on a failing mesh and must not be read as a phase diagram. The apparent best point (`chi = 3.0`: `protr` 1.430, `corr` +0.776) has **40% of faces folded**; it is not an operating point. Reproducing Okuda's (chi, gamma) diagram needs OR001 first.

### What I got wrong

- I predicted the **cap** was what held the shape flat, and that `rho = 0` would bulge.
  Wave B refuted it (`protr` 1.034, the lowest of its sweep) — but wave B ran against
  the dead field, so its refutation was uninformative. Wave E, with a live field,
  **confirmed** the cap reading. A refutation obtained under a broken instrument is not
  evidence, and I nearly recorded it as one.
- I predicted `protr >= 1.2` at `rho = 0.05` and got 1.173 — refuted by 0.027.
  Recorded as refuted rather than rounded into a pass.
- **Wave G refuted my timescale story.** I claimed `corr` would FALL as `rate` rose,
  because a pattern reorganising faster than the shell relaxes should decouple from it.
  Observed `corr`: 0.167 / 0.062 / 0.627 / 0.675 / 0.476 at `rate` 0.1 / 0.3 / 1 / 3 /
  10 — it RISES then falls, peaking near `rate ~ 3`. The low end is not decoupling at
  all: `act_max` there is 0.002 and 0.000, i.e. **the pattern had not formed yet in 400
  frames**. A run-length artefact I read as a mechanism until I checked `act_max`.
