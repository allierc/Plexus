# material_steps_1 -- auto-ported from prototype/water (water_steps_1.yaml).
general:
  name: material_steps_1
  seed: 0
  n_frames: 1000
  dt: 1.0
  boundary: wall
  obstacles:
  - [0.0, 0.0, 0.142, 0.46]
  - [0.142, 0.0, 0.285, 0.39]
  - [0.285, 0.0, 0.428, 0.32]
  - [0.428, 0.0, 0.571, 0.25]
  - [0.571, 0.0, 0.714, 0.18]
  - [0.714, 0.0, 0.857, 0.11]
  - [0.857, 0.0, 1.0, 0.04]
sets:
  cell:
    n: 1
    start: [0.1, 0.8, 0.1, 0.8]
    types:
      water:
        fraction: 1.0
        youngs: 300
        layers:
        - {frac: 1.0, youngs: 300, material: liquid}
  mpm_particle: {parent: cell, per_parent: 6000, radius: 0.2, density: 1.0}
fields: {}
operators:
- {op: aggregate, at: cell}
- {op: gravity, at: cell, g: 12.0}
- {op: mls_mpm_mechanics, at: mpm_particle, n_grid: 128, substeps: 18, dt_sub: 0.0002, a_max: 200, drag: 0.3, wall_damp: 0.4,
  wall_contact: 0.06, surface_tension: 30.0}
schedule: [aggregate, gravity, mls_mpm_mechanics]

# --- auto: video descriptions (gemma-4-12B) ---
descriptions:
  movie_cell:
    description: 'A blue, textured sphere is positioned in the upper left corner of the frame.

      A grey, stepped structure occupies the bottom left and center of the image.

      The blue sphere remains stationary while the grey steps appear to be a static background element.

      The overall scene is set against a plain white background with some faint blue textures appearing
      in the lower half.'
    objects: 'The blue sphere is a large, circular object with a mottled, icy texture.

      The grey structure consists of five distinct rectangular steps arranged in a descending staircase
      pattern.

      The steps are uniform in grey color and are connected to form a solid block at the bottom.

      The background is a large, white empty space that provides high contrast for the primary objects.'
  movie_mpm_particle:
    description: A dense cluster of blue particles is initially located in the upper-left corner of the
      frame. As time progresses, these particles begin to descend and spread out across the bottom of
      the screen. They interact with a series of gray, step-like platforms that appear to be descending
      as well. By the end of the sequence, the particles have settled into a thin layer along the bottom
      edge of the frame.
    objects: The blue particles are small, individual points that form a large, dense cluster at the start.
      These particles are light blue in color and move fluidly across the white background. The gray objects
      are large, solid, rectangular blocks arranged in a descending staircase formation. These gray steps
      are uniform in color and size, providing a structural boundary for the moving particles.
