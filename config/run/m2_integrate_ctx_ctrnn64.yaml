name: m2_integrate_ctx_ctrnn64
task: m2_integrate_ctx
circuit: {hidden: 64, tau0: 0.5, w_init: zeros}
training: {epochs: 200, n_trials: 512, n_val: 32, n_test: 64, lr: 0.01, batch: 32, seed: 0, curriculum: true,
  snapshot_every: 25, rollout_frames: 960, rollout_trials: 4, test_task: ''}
