# User Input — dicty model-discovery loop

The agent reads this every batch. Put live instructions under "Pending Instructions";
the agent acts on them, then moves them to "Acknowledged" with a timestamp.

## Pending Instructions

(none)

## Acknowledged (pending → acknowledged)

- 2026-06-18: Batch 35 read-back complete; no pending instructions at read time. **B35 PRODUCED THE PROJECT'S FIRST USABLE MPM DATA AND A DECISIVE STRUCTURAL NEGATIVE.** The launcher-independent `_autodetect_engine()` fix in `eval_sweeps.py` (B34) held — all 256 sims ran cleanly under the MPM engine. The result is the project's primary structural adjudication: **MPM with the bare {secrete, sense, inflow_mpm, mpm} stack is NON-AGGREGATING at every tested parameter (Est #130 DECISIVE).** sw 0 (n_frames at MPM base parent) shows inner_mass FLAT at 0.19 across n_frames in [120, 2400], morphology IDENTICAL sparse-scatter at every panel, n_mounds GROWS 43->79 (more scatter, not coalescence); loss is MONOTONE-UP 6.4->22.8 (longer sim = worse match). The finite-volume mechanism (intrinsic Young's modulus + grid-mediated exclusion) does NOT mitigate Est #82 — it replaces it with the OPPOSITE failure mode (under-aggregation). **The project's central structural hypothesis ("finite cell volume is the missing mechanism") is FALSIFIED in pilot.** Supporting falsifications: Est #131-#135 (MPM is parameter-inert for morphology across sense.gain, secrete.rate, camp.{D,decay}, mpm.drag — 5 axes, all flat); Est #136 + Est #144 (Young's modulus across 4 orders of magnitude does NOT produce structural change; numerical explosion at youngs>=2000); Est #139 METHODOLOGICAL — engine outer `dt` is structurally INERT in MPM (16 pixel-identical results across [0.10, 1.2]); root cause: MPM has its own dt_sub and substeps that gate cell physics, outer dt only affects cAMP grid. **DROP dt sweeps in future MPM batches.** PROJECT-BEST LOSS 1.141 at sw 11 mpm.substeps=32 AND loss=1.17 at sw 15 cell.youngs=5 (n_frames=1200) are BOTH SUSPECTED PARTICLE-DEPLETION ARTEFACTS (Est #140/#144 provisional) — morphology strips show progressively SPARSER fields, not aggregation; "low loss" reflects sparse-field SSIM coincidence with REAL density, not multi-mound match. Est #143 — MPM stack has 3x point-cell sigma noise floor (sigma~1.5 vs sigma~0.022); single-seed delta-loss < 1.5 is NOT real. **B36 PARENT — CONSERVATIVE refinements only:** per_parent 8->24 (Est #138), mpm.{a_max 1200->1800 (Est #141), vmax 6->1.5 (Est #142), drag 4->10 (Est #135)}, inflow_mpm.rate 1.5->0.5 (Est #134), secrete.rate 8->6, sense.gain 30->50 — all metric ripple, NO morphological adoption (suspected substeps=32 / youngs=5 wins NOT adopted pending adjudication). **B36 STRATEGIC FRAME — TWO PRONGS:** (alpha) ADJUDICATE Est #140/#144 via sw 0 substeps x n_frames=1200 stress test + sw 1 youngs x n_frames=1200 fine sweep + sw 3 16-seed verification at substeps=32 corner + sw 4 16-seed at the (substeps=32, youngs=5) JOINT corner (do the artefacts compound?); (beta) RULE OUT NARROW PRODUCTIVE CORNERS via sw 7-9 joint sweeps (sense.gain x camp.D, sense.gain x secrete.rate, mpm.a_max x camp.D) + sw 12 mpm.drag x n_frames=1200 + sw 13 cell.n x n_frames=1200 stress tests + sw 5 mpm.dt_sub (the live resolution knob, never swept). Parameter refinements at the new parent (sw 8-10, 14-15) confirm working values. **NO ENGINE CODE EDITS THIS BATCH** to preserve infra stability (B32/33/34 burned to code-spec mismatches). **B37 WILL ADD INTER-CELL COHESION TO MPM** — two options: (A) enable existing `mpm.surface_tension` (`mpm.py:194`) by adding `is_liquid` mask via a `cell.types.dicty.liquid` flag in `dicty_engine_mpm.build()` (small ~5-line edit); (B) dedicated MPM-native cell-cell adhesion operator (sigmoid-gated attraction, point-cell `spring` analogue). Either way, ablation = strength=0. **Flag for user:** the project has reached a CLEAN STRUCTURAL CONCLUSION — finite-volume cells alone are NEITHER NECESSARY NOR SUFFICIENT for multi-mound aggregation; the missing mechanism is INTER-CELL COHESION (intrinsic stiffness gives intra-cell volume only, no analog of point-cell `spring.kadh`). The B37 cohesion-mechanism edit IS the next experimental commitment. If B36 adjudication retracts Est #140/#144 cleanly (sw 0 collapses to empty field at substeps=32 with n_final < 100, sw 1 same for youngs=5), the project-best loss reverts to point-cell 0.9126 and B37 commits to mechanism addition without ambiguity. If sw 0/sw 1 surprisingly show morphology DEVELOPING under stress, Est #140/#144 are upgraded to real findings and parent adopts the corner. Either is a clean adjudication.

- 2026-06-18: Batch 34 read-back complete; no pending instructions at read time. **B34 PRODUCED NO USABLE DATA — THIRD CONSECUTIVE INFRASTRUCTURE FAILURE, IDENTICAL FAILURE FAMILY TO B32/B33.** All 256 sims aborted at the new `_preflight(base)` step with `ValueError: operator 'inflow_mpm' not in registry`; `sweep_results.json` is all-NaN; every `sweep_*.png` has an empty morphology strip; `best_montage.png` is stale from B30. **ROOT CAUSE — THE STALE LAUNCHER.** `dicty_loop_run.log` B34 banner prints `[loop] running 256 sims (16 sweeps x 16) on cuda:0 ...` — notice the ABSENCE of the `engine=<name>` token the patched on-disk `dicty_loop.py:81` was supposed to print. The on-disk autodetect in `dicty_loop.py:_engine_from_spec()` is present (mtime 03:47:54 ≪ B34 launch 03:58), but the long-running `dicty_loop.py` parent process loaded `dicty_loop.py` into memory BEFORE the B32 patch landed, so the in-memory `eval_sweeps()` is still the OLD version without `_engine_from_spec()`. Python does not re-import a running module from disk when its file changes. The spawned subprocess `eval_sweeps.py` IS fresh each batch, and its on-disk `_preflight()` correctly fired — exactly as designed, turning a silent 256-sim NaN bath into a loud, actionable error. **THE B33 USER-INPUT'S "PATCHES NOW LIVE FOR B34" CLAIM WAS WRONG**; the patches ARE on disk, but the long-running launcher process never reloaded them. **FIX APPLIED THIS BATCH — LAUNCHER-INDEPENDENT:** moved the engine autodetect from `dicty_loop.py:eval_sweeps()` to `eval_sweeps.py:_autodetect_engine()` at module top (before any operator import). Because `eval_sweeps.py` is launched as a fresh subprocess every batch, the on-disk version is always loaded — defeating the entire stale-launcher class of failure. Env override `DICTY_ENGINE` still wins. The redundant launcher-side autodetect in `dicty_loop.py` is left in place (cheap, idempotent) but is no longer load-bearing. The subprocess now prints `[eval_sweeps] DICTY_ENGINE=<name>` so future failures of this class will be loud at subprocess startup, not just at preflight. **NEW Est #129 (methodological):** engine autodetect must live in the spawned subprocess, not the launcher — a launcher process started before a patch landed will NEVER execute the new code regardless of how many batches elapse. The B32 fix's location was a meta-error; B34's fix puts the autodetect in the only place that is guaranteed-fresh. **KNOWLEDGE LEDGER UNCHANGED beyond Est #129** — B31's Est #115/#116 retractions + Est #117-#125 falsifications + Est #126 (point-cell engine structurally exhausted) + Est #127 (B32 preflight requirement) + Est #128 (B33 save-before-launch) all stand; no new mechanism tested in B32, B33, or B34. **`specs/dicty_loop_base.yaml` UNCHANGED** (still the MPM base spec — `cell.n=767`, `youngs=60`, `particle.per_parent=8`, `secrete+sense+inflow_mpm+mpm` schedule). **`sweep_plan.json` UNCHANGED** — the B32/B33/B34 plan (decisive n_frames break test, MPM-native parameter mapping, dt + mpm.substeps resolution probes, 16-seed verification, cell.youngs × n_frames=1200 stress test) is a well-designed first MPM survey and will re-run cleanly under the now launcher-independent autodetect as B35. **B35 = re-run the unchanged B32/B33/B34 plan.** **Flag for user:** the project schedule has now slipped THREE batches (B33+B34+B35 absorb what was planned as B32). The structural Est #82 break test at MPM engine level — the project's primary structural deliverable — is three batches behind, but the scientific question is unchanged. **Maximum-certainty option:** kill the running `dicty_loop.py` parent process and restart with `--resume`; then both launcher and subprocess will load the patched code paths cleanly. **Standard option:** trust the new launcher-independent autodetect — B35 should run because the load-bearing code is now in the spawned subprocess, which is always fresh. Either way, the failure mode is now closed in code, not by promise.

- 2026-06-18: Batch 33 read-back complete; no pending instructions at read time. **B33 PRODUCED NO USABLE DATA — SECOND CONSECUTIVE INFRASTRUCTURE FAILURE, IDENTICAL TO B32.** All 256 sims aborted at `set_param` with `operator 'inflow_mpm' not in registry. Available: [...point-cell ops...]`; `sweep_results.json` is all-`NaN`; every `sweep_<i>_<param>.png` is an empty morphology strip with only the REAL density tile populated; `best_montage.png` is stale from B30 (mtime 2026-06-18 03:30). **ROOT CAUSE — TIMESTAMPS TELL THE STORY:** `sweep_results.json` mtime 03:45:04 (B33 sims aborted); `dicty_loop.py` mtime 03:47:54; `eval_sweeps.py` mtime 03:48:26. The B32 agent's patches landed on disk AFTER B33's subprocess had already silently NaN'd all 16 sweeps. End of `dicty_loop_run.log` shows the newly-saved `_preflight()` raising a clean `ValueError` on the spec/engine mismatch, confirming the patches **are now live for B34**. **Two consecutive batches lost to the same launcher/spec mismatch.** **NEW Est #128 (methodological):** save-before-launch enforcement — when a batch agent edits the harness, the patches must be on disk before the next subprocess starts. Cheapest defense = a 1-sim canary against the current base spec asserted non-NaN before the 256-sweep launch (NOT implemented this batch to avoid further harness churn; deferred to user discretion). **KNOWLEDGE LEDGER UNCHANGED beyond #128** — B31's Est #115/#116 retractions + Est #117-#125 falsifications + Est #126 (point-cell engine structurally exhausted) + Est #127 (B32 preflight requirement) all stand; no new mechanism tested in either B32 or B33. **`specs/dicty_loop_base.yaml` UNCHANGED** (still the MPM base spec — `cell.n=767`, `youngs=60`, `particle.per_parent=8`, `secrete+sense+inflow_mpm+mpm` schedule). **`sweep_plan.json` UNCHANGED** — the B32/B33 plan (decisive n_frames break test, MPM-native parameter mapping, dt + mpm.substeps resolution probes, 16-seed verification, cell.youngs × n_frames=1200 stress test) is a well-designed first MPM survey and will re-run cleanly under the patched launcher as B34. **B34 = re-run the unchanged B32/B33 plan.** **Flag for user:** the project schedule has now slipped TWO batches (B33+B34 absorb what was planned as B32). The structural Est #82 break test at MPM engine level — the project's primary structural deliverable — is two batches behind, but the scientific question is unchanged. If you would like a hardened launcher (canary + fsync of harness edits before launch), say so; current behavior relies on the agent's Edit/Write being immediately durable, which empirically failed for B33.

- 2026-06-18: Batch 32 read-back complete; no pending instructions at read time. **B32 PRODUCED NO USABLE DATA — INFRASTRUCTURE FAILURE, NOT A SCIENTIFIC RESULT.** All 256 sims aborted at the first `set_param` call with `operator 'inflow_mpm' not in registry`. **ROOT CAUSE:** `dicty_loop.py:60` (`eval_sweeps()`) launched the subprocess without setting `DICTY_ENGINE`, so `eval_sweeps.py:26` defaulted to the point-cell engine `dicty_engine` even though `specs/dicty_loop_base.yaml` is now the MPM base spec (which schedules `inflow_mpm`, `mpm`, `particle:`). The point-cell engine has no MPM ops registered — every 256 sims aborted at launch. `sweep_results.json` is all-`NaN`; sweep figures have empty morphology strips. **THIS IS THE SAME INFRA-MISMATCH FAMILY AS B21 EST #72 AND B26 EST #95.** **FIXES APPLIED THIS BATCH:** (1) `dicty_loop.py:_engine_from_spec()` sniffs the base spec for MPM tokens and sets `DICTY_ENGINE=dicty_engine_mpm` for the subprocess; explicit env override still wins. (2) `eval_sweeps.py:_preflight(base)` aborts BEFORE the 256-sim loop if any scheduled operator is missing from `_OPERATOR_REGISTRY`, with a clear missing-op + loaded-engine diagnostic. **NEW Est #127 (methodological):** any base-spec/engine mismatch must fail at preflight — now enforced in code. The "I edited the spec but did not edit the launcher" pattern has now invalidated THREE batches; structurally fixed. **KNOWLEDGE LEDGER UNCHANGED** — B31's Est #115/#116 retractions + Est #117-#125 falsifications + Est #126 (point-cell engine structurally exhausted) all stand; no new mechanism tested. **`specs/dicty_loop_base.yaml` UNCHANGED** (still the MPM base spec; the plan was correct, only the launcher was broken). **`sweep_plan.json` UNCHANGED** — the B32 plan was a well-designed first MPM survey (decisive n_frames break test, MPM-native parameter mapping, resolution probes, seed verification, cell.youngs × n_frames=1200 stress test); it will re-run cleanly under the patched launcher. **B33 = re-run the unchanged B32 plan.** The structural Est #82 break test at MPM engine level — the project's primary structural deliverable — is DEFERRED ONE BATCH; the scientific question is unchanged. **Flag for user:** the project schedule slips by one batch (B33 absorbs what was planned as B32). If the harness ever again produces this kind of silent all-NaN result, the new `_preflight` will name the missing op and the loaded engine, allowing a one-step fix without burning 256 sims. If the user prefers a hard ban on running with `DICTY_ENGINE` unset (i.e., require explicit env), say so — current behavior is to auto-detect, which is convenient but slightly more magical.

- 2026-06-18: Batch 31 read-back complete; no pending instructions at read time. **B31 DECISIVELY CLOSES THE POINT-CELL ENGINE — Est #113 RETRACTED, MPM FORK CONFIRMED FOR B32.** 256 sims at B30 parent (D=0.0042). **(1) THE CRITICAL TEST RESOLVED (sw 0, DECISIVE):** n_frames at the BARE NEW PARENT shows EXACT Est #82 trajectory — nm 10→6→3→1 by n_frames=480, inner_mass 0.351→0.895 monotone-UP, single-blob+halo speckle morphology at ALL endpoints to n_frames=2400. **Est #113 (D=0.0042 multi-mound new parent) RETRACTED as METRIC ARTEFACT (NEW Est #115)** — SECOND case in two batches of "16-seed verified parent" turning out to be a transient (after B30 Est #112 retracted Est #104). Methodological lesson: the only valid Est #82 break test is n_frames≥1200 at the BARE parent, not at the evaluation frame the parent was calibrated to. **(2) Est #114 (decay=0.02 niche) RETRACTED (NEW Est #116):** sw 1 collapses same way, ~120 frames delayed. **(3) FOUR INDEPENDENT n_frames=1200 STRESS TESTS UNIVERSALLY COLLAPSE — Est #82 PARAMETER-INVARIANT under the new parent:** sw 8 cell.n [800, 5000] → nm=1 (NEW Est #121); sw 9 kadh [5, 35] → nm=1 (NEW Est #122); sw 11 camp.diffusion [0.0008, 0.015] → nm=1 (NEW Est #124 — **DECISIVELY closes the "D shift mitigates Est #82" hypothesis** that motivated the last 6 batches' parent); sw 15 r_on [0.185, 0.25] → nm=1 in productive band (NEW Est #125). **(4) B30 SINGLE-SEED WINS NOT REPLICATED:** sw 2 16-seed at kadh=9 median ≈ 0.95 σ ≈ 0.022 (B30 sw 8 best 0.9055 was 1/16 seed-luck; NEW Est #117 — THIRD case of single-seed retraction after Est #97/#104); sw 3 16-seed at D=0.002 indistinguishable from D=0.0042. **(5) PARAMETER REFINEMENTS UNIVERSALLY FLAT** (sw 4-7, 12-14): D corridor [0.0012, 0.006] (NEW Est #118), camp.decay [0.03, 0.5] (NEW Est #119), kadh [5, 18] (NEW Est #120), cell.n [1100, 3000], sat_n [1.5, 3.0], c_sat [0.3, 1.5], inflow [0.5, 12] — all broad parent-tied plateaus, NO sharp optima. **(6) HIGH-DECAY ANOMALY (sw 10):** decay≥2.0 at n_frames=1200 opens a NEW dispersion regime (gradient annihilation, nm=30-60 sparse-scatter) — confirms Est #68 family at bulk-decay level but is catastrophic loss, NOT productive (NEW Est #123). **PROJECT-BEST LOSS UNCHANGED at 0.9126** (sw 4 D=0.0012 ties old B30 parent value — the 6th tie at this floor since B17, confirming the loss floor is metric-bound, not config-discoverable in this engine). **(7) NEW Est #126 STRATEGIC: THE POINT-CELL ENGINE IS STRUCTURALLY EXHAUSTED.** 12 mechanisms tested across 31 batches, 11 falsified (only sense_sat survives as regularizer). The Est #82 runaway-compaction failure is now confirmed parameter-invariant across ALL tested parameters at the new parent. The n_frames=400 evaluation point gates the entire model within a TRANSIENT phase — multi-mound morphology exists only as a kinematic transient with no stable attractor. **B32 COMMITS TO THE MPM ENGINE FORK.** MPM cells have intrinsic finite-volume incompressibility (Young's modulus 60, true volume exclusion via the grid; particles cannot overlap below particle radius) which addresses Est #82 AT ITS STRUCTURAL ROOT — not via operator augmentation. B32 PARENT = `specs/dicty_mpm_base.yaml`. **B32 PRONGS:** (μ) DECISIVE n_frames at MPM base parent (sw 0) — the structural Est #82 break test at engine level; if multi-mound persists to n_frames≥1200, Est #82 RESOLVED at engine level (the project's primary structural deliverable). (ν) MPM-NATIVE parameter sweeps (sw 1-10: sense.gain, secrete.rate, camp.{diffusion, decay}, inflow_mpm.rate, mpm.{drag, vmax, a_max}, cell.{n, youngs}, particle.per_parent) — map the MPM parameter surface around the base parent. (ξ) Engine-resolution probes (sw 11 dt, sw 12 mpm.substeps) — Est #9 / Est #81 analogues for MPM. (ο) 16-SEED verification at MPM parent (sw 15) + cell.youngs × n_frames=1200 STRESS TEST (sw 16, the high-Young's Est #82 break variant — IF Young's=200 preserves morphology where Young's=60 does not, structural-stiffness is the key Est #82 mitigator). **Flag for user:** B32 is the PROJECT'S STRUCTURAL TEST. If MPM sw 0 sustains multi-mound morphology to n_frames=1200 with visible distinct dense compact mounds (not halo speckle), the PROJECT'S CENTRAL STRUCTURAL HYPOTHESIS — that finite cell volume is the missing mechanism for Est #82 mitigation — is CONFIRMED, and the next batches refine MPM toward REAL morphology match. If MPM ALSO collapses, the missing mechanism is in the chemotactic SOURCE+SENSE dynamics or the metric (Est #42 family), not the cell representation; this would be a deeper structural negative result and motivates either (a) bare relay-on chemotaxis in MPM (necessary mechanism test under finite-volume cells) or (b) METRIC AUGMENTATION (the project's loss floor 0.9126 has been tied 6 times at structurally distinct parents in 6 batches and may be reaching its metric ceiling). Either outcome is a clean adjudication; B32 is positioned as the project's primary structural deliverable.

- 2026-06-18: Batch 30 read-back complete; no pending instructions at read time. **B30 PRODUCED TWO DECISIVE FINDINGS — ONE RETRACTION + ONE BREAKTHROUGH.** 256 sims at B30 parent (B29 parent + D=0.0042). **(1) DECISIVE Est #104 RETRACTION (Est #112):** sw 0 16-seed verification at the Est #104 config (corner + decay=1.4 + n_frames=1200) NUMERICALLY PASSED (12/16 nm>=4, median loss ~1.00) BUT morphology strip shows SINGLE TIGHT CENTRAL BLOB + halo speckle at EVERY seed — peak detector counts halo scatter as "mounds". sw 2 (n_frames at Est #104) decisively reveals the configuration is a DELAYED TRANSIENT: nm trajectory 17→25→13→7→2→1→1→1 as n_frames extends 200→2400, inner_mass monotone-UP to 0.44, EXACTLY the Est #82 collapse just delayed ~400 frames. sw 3 (alternative r_on=0.222 corner) reconfirms; sw 4/14 (capacity), sw 12 (density_repel stack), sw 13 (c_sat), sw 15 (sat_n) all monomorphic-blob. **Est #104 is NOT a true Est #82 mitigation; it is a metric artefact.** Methodological lesson: peak-count metric gameable by halo speckle — 16-seed numeric verification is NECESSARY but NOT SUFFICIENT; visual morphology strip is the final adjudicator. **(2) DECISIVE Est #113 BREAKTHROUGH — D=0.0042 NEW PARENT IS THE FIRST POINT-CELL PARENT IN 30 BATCHES TO REPLICATE GENUINE MULTI-MOUND MORPHOLOGY ACROSS 16 SEEDS:** sw 5 (16-seed at bare new parent): loss [0.92, 1.04], median ~0.945, σ≈0.026 (tightest of project); morphology strip shows 4-8 DISTINCT DENSE COMPACT MOUNDS at EVERY SEED, visually resembling REAL. Independent corroboration: sw 6 D corridor broadens to [0.001, 0.012] (Est #99 BROADENED); sw 7 r_on corner DISSOLVES — broadly productive [0.17, 0.24] (Est #98 D-conditional); sw 8 kadh broadly productive [5, 35] with NEW PROJECT-BEST LOSS 0.9055 at kadh=9 (single-seed, needs verification); sw 9 gain plateau D-invariant; sw 10 productive n=[1100, 3000] with Est #93 high-n collapse at n>=3500 holding; sw 15 sat_n plateau D-invariant. **(3) Est #109 PROMOTED to Est #114:** sw 11 16-seed at decay=0.02 reproduces multi-mound at most seeds (7/16 morph<=0.25, project-second-best 0.9090 seed=8). Low-decay morph niche VERIFIED across seeds. **(4) PROJECT-BEST LOSS 0.9055 at kadh=9 (single-seed):** marginal improvement (Δ=0.001 < σ=0.026); NEEDS 16-seed verification before parent adoption per Est #97/#104 lessons. **B31 PARENT UNCHANGED from B30 (no single-axis change verified)**. **B31 STRATEGIC FRAME — THE CRITICAL UNRUN TEST.** B30 sw 2 (n_frames sweep) was at the Est #104 CORNER, NOT at the bare new parent. The decisive question for B31 is: does the D=0.0042 multi-mound morphology SURVIVE to n_frames=1200, or is it (like Est #104) just a delayed transient? **B31 PRONGS:** (α) DECISIVE n_frames sweep at the BARE NEW PARENT (sw 0) + at decay=0.02 variant (sw 1) — Est #82 break test at the productive regime; (β) 16-seed verifications of single-seed B30 wins: sw 2 kadh=9 (project-best), sw 3 D=0.002 (sw 6 winner); (γ) refine bands under new D (sw 5-8 FINE) + stress-test multi-mound robustness at n_frames=1200 across cell.n/kadh/decay/D/r_on joints (sw 9-12, 16). Two MORPH refinements (sw 13 sat_n, sw 14 c_sat) + inflow re-pin (sw 15, untested at new parent since B14). **Flag for user:** B31 is the ENGINE-LEVEL ADJUDICATION batch. If sw 0 (DECISIVE n_frames at bare new parent) shows multi-mound morphology PRESERVED to n_frames=1200+ — visually distinct dense compact mounds, NOT diffuse halo/single-blob — Est #82 is FINALLY mitigated by structural D shift and **the MPM ENGINE FORK is DEFERRED INDEFINITELY**. The point-cell engine has a non-trivial structural Est #82 mitigation via parameter shift alone. If sw 0 shows the SAME COLLAPSE TRAJECTORY as Est #104 (monotone nm decay to 1, single-blob morphology by n_frames=1500+), the multi-mound regime at n_frames=400 was just a longer transient and **the MPM ENGINE FORK is the principled escalation**. Either outcome is a clean adjudication. Note: Est #112 is the SECOND case in 30 batches where a numeric verification passed but morphology failed (B27 sw 2 / Est #97 was the first, retracted in B28). The pattern motivates a possible refinement of the morph_score peak detector in B32+ to gate on per-peak intensity (filter out halo speckle).

- 2026-06-18: Batch 29 read-back complete; no pending instructions at read time. **B29 PRODUCED A SURPRISE FINDING — FIRST POINT-CELL Est #82 PARTIAL RESCUE.** 256 sims at B29 parent (B28 parent + het_std reverted to 0 per Est #101). **(1) NEW Est #104 — THE BIG FINDING:** sw 4 (8-mound corner: r_on=0.19, kadh=20, gain=1500 + camp.decay=1.4 + n_frames=1200) produced **nm=6, inner_mass=0.373, loss=0.9863** — the FIRST point-cell config to break Est #82 since B24. CORNER × DECAY interaction (sw 3 at parent + same decay still collapses to nm=1, so the decay rescue is corner-conditional). Mechanism hypothesis: faster cAMP turnover prevents global-attractor formation while corner-adhesion maintains local mound structure. **REQUIRES 16-seed verification in B30** before parent adoption (B27/B28 Est #97/#101 lesson). If verified, the MPM ENGINE FORK (queued in `sweep_plan_mpm.json`) is DEFERRED. **(2) NEW Est #105 — D=0.0042 ROBUST MORPH ANCHOR:** sw 6 (16-seed at D=0.0042) gave 4/16 seeds at morph_score=0 (nm=8) and 7/16 at morph<=0.13 vs sw 0 parent's 0/16 morph=0. Sigma_loss=0.03 (tightest of B29). **B30 PARENT adopts camp.diffusion=0.0042** (single-axis conservative change). **(3) FOUR NEW Est #82 FALSIFICATIONS:** sw 7/8 — sense_sat.gain attenuation FAILS to halt collapse at parent and at corner (Est #106 — 12th project mechanism falsified; chemotaxis SOURCE attenuation is the structurally-distinct counterpart of B28 adh_cap SINK attenuation, both now dead); sw 9 — RW × n_frames=1200 universally nm=1 (Est #107); sw 10 — vmax × n_frames=1200 universally nm=1 (Est #108); sw 12 — STACKED density_repel × 8-mound corner × n_frames=1200 universally nm=1-2 (Est #110). **The operator-side rescue family CANNOT halt Est #82 — Est #104 is now the ONLY remaining mitigation candidate within the engine.** **(4) Est #103 — new r_on=[0.220, 0.225] candidate corner (B28 sw 8) FALSIFIED as corridor:** sw 1/2 — single-bin morph wins move with seed; r_on=0.222 at corner gives morph=0.51 nm=4. The r_on=0.222 single-bin behavior persists in seeds-distribution (sw 5: 5/16 seeds morph<=0.13; slightly above bare parent's 2/16) and at cell.n=2200 (sw 15 morph=0.0005) but is NOT a robust corridor in r_on. **(5) Est #109 — low-decay morph niche at decay=0.02 (sw 11):** single-seed morph=0.0002 nm=8 loss=0.9572. Requires 16-seed verification. **(6) Est #111 — Est #100 GENERALIZED:** sw 14 — the NEW r_on=0.222 corner collapses FASTER (~360 frames) than Est #98 r_on=0.19 corner (~750 frames). All tested 8-mound corners are kinematic transients. **(7) PROJECT-BEST LOSS marginally improved 0.9066 at sw 5 seed=7 r_on=0.222** (Δ=0.006 < σ=0.04, NOT adopted as parent — within seed noise). **B30 PARENT: camp.diffusion 0.0012 → 0.0042 (Est #105 verified single-axis change)**; otherwise unchanged. **B30 STRATEGIC FRAME — THREE PRONGS:** (μ) Est #104 16-seed VERIFICATION (sw 0 DECISIVE seed-luck-vs-mechanism test) + mechanism-edge probes (sw 1 decay FINE at corner, sw 2 n_frames at Est #104, sw 3 Est #104 × NEW r_on=0.222 corner, sw 4 cell.n at Est #104); (ν) D=0.0042 NEW PARENT REFINEMENT (sw 5 16-seed re-pin, sw 6 D FINE, sw 7 r_on under new D, sw 8 kadh under new D, sw 9 sense_sat.gain, sw 10 cell.n); (ξ) FINAL Est #82 ADJUDICATION PROBES (sw 11 Est #109 16-seed verification, sw 12 decay × n_frames=1200 at r_on=0.222 corner, sw 13 density_repel at Est #104, sw 14 c_sat × Est #104, sw 15 sat_n × Est #104). **Flag for user:** B30 is the Est #104 ADJUDICATION batch. If sw 0 16-seed verification PASSES (>=3/16 seeds with nm>=4, median loss within parent noise of 0.9863), B31 adopts (corner + decay=1.4) as the new parent and the MPM fork is DEFERRED — the point-cell engine has a non-trivial Est #82 mitigation. If sw 0 FAILS verification, Est #104 RETRACTS as 1/16 seed-luck (mirror of Est #97 retraction in B28) and B31 commits to the MPM engine fork (`dicty_engine_mpm.py` + `sweep_plan_mpm.json` already queued). Either outcome is a clean adjudication closing the point-cell engine question.

- 2026-06-18: Batch 28 read-back complete; no pending instructions at read time. **B28 RETRACTS Est #97 AND FALSIFIES THE ADH_CAP PILOT — TWO DECISIVE NEGATIVES.** 256 sims at B27 parent (Est #97 verification batch). **(1) PRONG (ζ) Est #97 VERIFICATION FAILS — NEW Est #101.** sw 0 16-seed distribution at secrete_het.het_std=0.20: loss [0.909, 1.043], sigma≈0.036 (matches parent noise floor), median ~0.96; BUT morph_score WILD — only 4/16 seeds at morph<=0.05 (seeds 0/11 morph≈0.0001-0.0003 nm=8; seeds 5/14 morph≈0.125 nm=9/7); the other 12 seeds spread morph in [0.125, 0.63] with nm in [3, 11]. The B27 sw 2 nm=8 morph=0.0001 finding was 1/16 SEED-LUCK. Across sw 1-4 (het FINE + 3 densification joints): ablation wins or ties on loss at every joint; no productive het niche under morph_score either. **B19 Est #63 (secrete_het falsified) STANDS REINSTATED under BOTH metrics; secrete_het is the 11th project mechanism falsified, 7th cell-side. Est #80 (operator-side family exhausted) holds.** Methodological lesson: single-seed-deep findings (1/16) require 16-seed distribution tests before adoption (mirrors B26 Est #95 silent-op caution). **(2) PRONG (η) ADH_CAP PILOT FAILS — NEW Est #102.** sw 15 spring.kadh × cell.n=3000 × n_frames=1200: kadh=0 EXPLODES (loss 18.7 nm=69 sparse-scatter); kadh in [1, 200] universally collapses to nm=1 with inner_mass 0.48-0.86, loss [1.22, 1.36]. Low kadh does NOT halt Est #82 because chemotactic pull dominates regardless of adhesion. **The adh_cap design (per-cell rho gate ATTENUATES spring adhesion when rho>thr) FAILS in pilot — gating adhesion off in dense regions provides NO mitigation because collapse is sense_sat-driven, not adhesion-driven.** Independent confirmation that Est #82 is not rescued by EITHER het (sw 5: every het value at n_frames=1200 collapses to nm=1) NOR kadh-attenuation (sw 15). **(3) Est #98 8-mound corner RECONFIRMED AND BROADENED:** gain band wide [600, 3500] (sw 6, ~6× span), kadh band [10, 22] (sw 7), r_on band [0.183, 0.198] AND NEW candidate window r_on=[0.220, 0.225] (sw 8); Est #99 D corridor under het=0.20 [0.0018, 0.0042] (sw 10). **(4) Est #100 GENERALIZED:** corner collapse timescale ~750 frames is GAIN-INVARIANT (sw 14 at gain=750 collapses identically to B27 sw 15 at gain=2500). **(5) pulse_dens (Est #80) reconfirmed catastrophic across all thr in [0.5, 8.0] (sw 13) — failure is AMPLITUDE-driven not threshold-driven.** **(6) Est #93 high-n ceiling holds even with stacked het + density_repel rescue (sw 12, n>=3500 collapses).** **PROJECT-BEST LOSS = 0.909 at sw 0 seed=5 het=0.20** — within seed noise of parent 0.9126 (Δ=0.0036 < σ≈0.036); marginal, NOT a genuine improvement; does NOT motivate parent adoption. **B29 PARENT: revert het_std=0 (per Est #101); otherwise unchanged.** **B29 STRATEGIC FRAME — FINAL POINT-CELL CLOSING BATCH.** After 28 batches the OPERATOR-SIDE MECHANISM FAMILY is DEFINITIVELY EXHAUSTED (11 mechanisms falsified across batches B5-B23 + B19/B28 re-falsification of secrete_het). NO single operator rescues Est #82 (density_repel via Est #90, kadh-attenuation via Est #102). The cleanest 8-mound corner (Est #98) is a SHORT (~750-frame) gain-invariant transient (Est #100). **B29 has TWO PARALLEL PRONGS within the point-cell engine:** (a) **FINAL CORNER+TIMESCALE adjudication** — refine the NEW Est #98 candidate r_on=[0.220, 0.225] (sw 1-3, 14-16), test whether high camp.decay extends Est #100 transient (sw 4-5, 12), seed-verify Est #99 D=0.0042 corridor (sw 7), seed-verify project-best parent reproducibility (sw 0, 6); (b) **FINAL OPERATOR-SIDE TESTS** — DECISIVE sense_sat.gain-attenuation × n_frames=1200 break test (sw 8-9, the ONLY structurally-distinct remaining operator-side Est #82 candidate after Est #102 falsified the sink-attenuation route — tests whether attenuating the chemotaxis SOURCE rescues where adhesion attenuation could not); high-noise random_walk × n_frames=1200 (sw 10, untested for 10 batches); extreme vmax × n_frames=1200 (sw 11, untested Est #66 × Est #82 joint); stacked corner + density_repel × n_frames=1200 (sw 13, the final stacked Est #82 test). **THE MPM ENGINE FORK ESCALATION IS THE B30 PLAN.** `dicty_engine_mpm.py` already exists in the repo with `mpm_best_montage.png` and `mpm_sweep_*` artefacts; `sweep_plan_mpm.json` is the separate MPM arm activated via `DICTY_ENGINE=dicty_engine_mpm`. If B29 closes flat (no new productive niches, sense_sat.gain attenuation also fails Est #82, no new corner morph win replicates across seeds), the project's structural conclusion is SEALED at the engine level and B30 ports parent to MPM for the decisive native finite-volume Est #82 break test. **Flag for user:** B29 is positioned as the LAST point-cell batch; the deliverable is a CLEAN CLOSING ADJUDICATION of (i) sense_sat-source-attenuation as the final operator-side Est #82 candidate, (ii) the NEW r_on=[0.220, 0.225] candidate corner replication across seeds, and (iii) Est #99 D=0.0042 replication across seeds. If ANY of these three opens a new productive niche, B30 stays on point-cell and tests it further; otherwise B30 is MPM fork.

- 2026-06-18: Batch 27 read-back complete; no pending instructions at read time. **B27 PROJECT-BEST LOSS UNCHANGED at 0.9126** (sw 0 seed=0, sw 1 r_on=0.20, sw 10 sat_n=2.1, sw 12 relay.gain=140 all tied). **(1) STRONGEST NEW FINDING — Est #97:** sw 2 secrete_het.het_std=0.20 produced nm=8 morph=0.0001 loss=0.9152 (within seed-noise of parent). B19 had falsified secrete_het by SSIM-loss alone; under morph_score with the Est #95 silent-op bug fixed, a NARROW productive window het_std ∈ [0.10, 0.25] reaches the 8-mound morph manifold at parent-tied loss. **Est #63 (secrete_het falsified) PARTIALLY RETRACTS.** This is the FIRST candidate productive interior mechanism added to the model in 27 batches; verification pending. **(2) PRONG (β-redo) OTHERWISE HOLDS:** decay_dens (sw 3), pulse_dens (sw 4), diff_dens (sw 5) re-evaluation under morph_score with spec NOW correct — all three falsifications HOLD. Est #68/#80/#78 confirmed under both metrics. **(3) PRONG (δ/ε) 8-MOUND CORNER VINDICATED — Est #98:** sw 8 reveals (r_on=0.19, kadh=20, gain ∈ [750, 3500]) is a 4-point morph_score=0 corridor; c_sat=1.0 transfers cleanly (sw 9). Loss-cheapest 8-mound config of the project: gain=750 → loss=0.9316 (1.04× parent). **(4) PRONG (γ) DECISIVE — Est #100 NEW:** sw 14 reconfirms Est #82 collapse at parent (nm=1 by n_frames=1050); **sw 15 at the 8-mound corner shows the corner collapses FASTER (nm=1 by n_frames=750, ~300 frames earlier).** Higher gain → stronger chemotactic pull → faster runaway. The cleanest 8-mound config has the SHORTEST stable-multi-mound transient of the project. The 8-mound morphology is a TRANSIENT, never an equilibrium. **(5) Est #99 NEW:** productive camp.D corridor for 8-mound = [0.0022, 0.0055] (sw 13, sharper than the loss-flat band); current parent D=0.0012 sits just BELOW. **(6) Est #93 REFINED:** density_repel.strength=4 extends rescue to n=4500 but DESTROYS low-n configs (n<=600 explosive). **OTHER MORPH CANDIDATES:** sw 1 r_on=0.185 morph=0 nm=8; sw 10 sat_n=1.4 morph=0.0001 nm=8; sw 12 relay.gain=290 morph=0 nm=8. **B28 PARENT UNCHANGED** — the Est #97 finding is single-seed-deep and requires 16-seed verification before adoption (mirrors B26 Est #95 caution). **B28 STRATEGIC FRAME — TWO PRONGS:** (ζ) **VERIFY Est #97** via 16-seed sweep at het_std=0.20 (sw 0, DECISIVE seed-luck-vs-mechanism test; analogue of B22 sw 10, B23 sw 8) + narrow het FINE (sw 1) + het × densification joints (sw 2-4) + het × 8-mound corner (sw 5) + het × n_frames=1200 (sw 6, DECISIVE Est #82 break test under het-on). If verified, B29 adopts het_std=0.20 as parent and the operator-side family exhaustion (Est #80) partially retracts; (η) **DRAFT adh_cap (mass-cap adhesion)** design — per-cell local rho gate that ATTENUATES spring adhesion when rho > thr (REMOVES adhesion in dense regions; distinct from density_repel which adds outward force and FAILED Est #82). sw 16 is the adh_cap PILOT: spring.kadh × cell.n=3000 × n_frames=1200 — if low kadh (≤5) produces stable multi-mound, the operator design is on track for B29 implementation. **Flag for user:** if Est #97 verification PASSES (sw 0 distribution tied with parent within sigma≤0.06 AND most seeds nm=8 morph≤0.05), het_std=0.20 enters the B29 parent and the operator-side family exhausted claim (Est #80) partially retracts (3rd productive cell-side mechanism after sense_sat and density_repel-rescue). If verification FAILS (distribution wider than parent, OR morph_score does not replicate at most seeds), Est #97 REVERTS as single-seed luck and B29 escalates directly to adh_cap implementation OR MPM engine fork. Additionally, sw 14 (pulse_dens.thr at amp=1.0) tests whether the B27 sw 4 catastrophe at thr=2.0 was threshold-driven or amplitude-driven; sw 16 doubles as the adh_cap design proxy and the operator-side Est #82 final test. Note: per the B26 silent-op lesson (Est #95), continue verifying any swept parameter is bound to a scheduled operator before launching; the four ablation operators remain in the B28 base spec at zero strength.

- 2026-06-18: Batch 26 read-back complete; no pending instructions at read time. **B26 — PRONG (β) INVALID DUE TO SPEC BUG; PRONGS (α) AND (γ) DECISIVE.** 256 sims at the B26 parent (B25 parent + spring.r_on 0.20 -> 0.19). **(1) SPEC BUG — Est #95.** Four of 16 sweeps (sw 8/9/10/11 secrete_het/decay_dens/pulse_dens/diff_dens RE-EVALUATION under morph_score) returned BIT-IDENTICAL results — loss=0.98, inner=0.293, nm=5, morph=0.376 — across all 16 values per sweep. Root cause: the four operator instances were NOT in `specs/dicty_loop_base.yaml`'s `operators:` or `schedule:` lists ("kept in code as ablations" only). `eval_sweeps.set_param("<op>.<param>", val)` iterates `sc.operators` to find op.op==head, finds nothing, returns silently. This is the SECOND silent-operator bug in the project (after B21 Est #72 `fld.diffusion` vs `fld.D`). The B19-B23 falsifications stand UNTESTED under morph_score; prong-β is UNADJUDICATED in B26. **(2) PRONG (α) 8-MOUND MANIFOLD STRONGLY SUPPORTED (Est #92).** Every informative B26 sweep produced nm=8 morph<=0.001 at multiple values: sw 2 (r_on=0.19 × kadh=45 morph=0.0029), sw 3 (r_on=0.19 × c_sat=0.9 morph=0.0), sw 5 (cell.n=2200 morph=0.0002), sw 14 (r_on=0.19 × kadh=20 × gain=2500 morph=0.0). Joint transitivity holds across multiple axes; the B15 Est #43 joint-collapse concern is dissolved under morph_score. Cleanest 8-mound corner: (r_on=0.19, kadh=20, gain in [750, 2500]). **(3) PRONG (γ) Est #82 RUNAWAY COMPACTION RECONFIRMED at r_on=0.19 (Est #96).** sw 7 (n_frames sweep at r_on=0.19): loss monotone-UP 0.94->1.29; inner_mass monotone-UP 0.27->0.84; nm collapses 11->1 by n_frames=560. The "sparser adhesion reach r_on=0.19 resists runaway compaction" hypothesis is FALSIFIED — collapse trajectory identical to B24 sw 6/14 at r_on=0.20-0.245. sw 6 (density_repel × n_frames=1200, thr=0.5): NO STRENGTH HALTS THE COLLAPSE (Est #94) — strengths <=6 give single-blob (loss 1.18); strengths >=12 give explosive uniform dispersion (loss 15-20). density_repel is NOT a finite-volume fix even with thr=0.5 always-on gating; the B25 "operator was just gated too late" hypothesis is FALSIFIED. **(4) Est #89 PARTIALLY RETRACTED (Est #93).** sw 5 (cell.n WIDE at density_repel.strength=2.0) shows high-n single-blob REAPPEARS past n>=4000 even with rescue on; B25 rescue narrows to n<=3000. **(5) Est #91 RECLASSIFIED.** r_on=0.19 morph "winner" is a flat morph plateau across r_on=[0.195, 0.20]; B27 parent REVERTS to r_on=0.20 (project-best loss=0.9126 tie). **PROJECT-BEST LOSS UNCHANGED at 0.9126** (sw 1 r_on=0.20). **B27 PARENT: spring.r_on 0.19 -> 0.20 + SPEC FIX (secrete_het/decay_dens/pulse_dens/diff_dens NOW SCHEDULED at ablation defaults).** **B27 STRATEGIC FRAME — THREE PRONGS:** (β-redo) PROPER prong-beta re-evaluation under morph_score with the four operators now actually scheduled (sw 3-6); (ε) DRILL the 8-mound corner via (r_on=0.19, kadh=20, gain/c_sat) sub-corner sweeps (sw 9, 10, 16); (γ) continue Est #82 break tests at B27 parent (sw 15) AND at the 8-mound corner (sw 16). **adh_cap (mass-cap adhesion as next Est #82 candidate) DEFERRED to B28 pending dicty_ops.py implementation.** **Flag for user:** if all four re-evaluations return flat under morph_score with the spec now correct, the operator-side family is DEFINITIVELY exhausted under both metrics and B28 escalates to EITHER adh_cap operator implementation OR the MPM engine fork (`dicty_engine_mpm.py`). Additionally, the silent-op pattern that has now invalidated sweeps in TWO batches (B21 Est #72, B26 Est #95) deserves a programmatic pre-flight check in `eval_sweeps.py` that verifies each swept parameter is bound to a scheduled operator instance before launching 16 sims.

- 2026-06-17: Batch 25 read-back complete; no pending instructions at read time. **B25 BREAKTHROUGH IS ON THE METRIC, NOT THE MECHANISM.** 256 sims at the B25 parent (B24 parent + new `density_repel` operator strength=0 ablation + morph_score newly implemented in `eval_sweeps.py`). **(1) METRIC AUGMENTATION FINALLY ADJUDICATES Est #42 / Est #58.** `morph_score = w_peak·|n_mounds_sim - 8|/8 + w_dens·per_spot_density_MSE` is implemented and reported alongside loss in sweep_results.json. The metric IMMEDIATELY revealed hidden 8-mound configurations across NINE sweeps (sw 1, 2, 3, 4, 8, 9, 10, 11, 13, 14, 15) with morph_score≤0.005 — the parameter cube ALREADY contained 8-mound configs from at least B17 onward, hidden by the SSIM-loss smooth-density bias. **Est #58 5-7 mound STRUCTURAL CEILING — RETRACTED as METRIC ARTEFACT** (now Est #87). The morphology gap vs REAL is now per-spot density and spatial arrangement, NOT mound count. **(2) `density_repel` IS SUFFICIENT BUT NOT NECESSARY.** Sw 1: strength=0.05-0.55 → nm=8 (morph≤0.002); but ablation (strength=0) ALSO reaches nm=8 at multiple other parents (r_on=0.19, r_on=0.23, kadh=20, c_sat=0.8/1.2). Catastrophe at strength≥6 (cells flooded by repulsion). Productive band [0.02, 3.5] (Est #88). **(3) CLEAN PRODUCTIVE ROLE FOR density_repel:** sw 5 at cell.n=3000 — without density_repel the model collapses to a SINGLE TINY POINT (nm=1); with strength=0.5-3.5 multi-mound is restored (nm=4-9). Est #89: density_repel is THE rescue for the Est #71/#84 high-cell.n single-attractor failure. **(4) FAILS Est #82 BREAK TEST.** Sw 6 at n_frames=1200: every strength in [0, 6] collapses to a single tiny central point; density_repel does NOT prevent the runaway compaction in this form (Est #90). Engine fork (MPM) remains live. Possibly the operator gate (thr=1.5×mean ρ) fires too late; B26 sw 7 re-tests with thr=0.5 (always-on density-spacer). **(5) NEW B26 PARENT: spring.r_on = 0.20 → 0.19** (Est #91). Sw 8 r_on FINE found r_on=0.19 morph=0.0003 nm=8 loss=0.9277 — strongest single-axis morph signal of the batch, 400× below parent's morph=0.125. r_on=0.23 (Est #85) also delivers morph=0.005 nm=8 — VINDICATED. Project-best loss UNCHANGED at 0.9126; project-best morph_score ≈ 0 at sw 15 strength=3.5 (kadh=20, nm=8, loss=0.9381). **B26 STRATEGIC FRAME — THREE PRONGS:** (α) MAP the 8-mound morph manifold (joint transitivity: r_on × kadh sw 3, r_on × c_sat sw 4); (β) RE-EVALUATE the four B19-B23 falsified mechanisms (secrete_het sw 9, decay_dens sw 10, pulse_dens sw 11, diff_dens sw 12) under morph_score — they were ruled out by a metric we now know was blind to mound count; some may now be productive; (γ) RE-TEST Est #82 at the new parent (sw 7 density_repel × n_frames=1200 with thr=0.5; sw 8 n_frames sweep at r_on=0.19). **Flag for user:** B26 is a "re-litigation" batch. If any of decay_dens / secrete_het / pulse_dens / diff_dens shows a sharp morph_score interior optimum where SSIM-loss is flat, that mechanism returns to OPEN and Est #80 ("operator-side mechanism family exhausted") is partially retracted. If all four remain flat under morph_score, the falsifications stand and the only remaining structural concern is Est #82 runaway compaction. If sw 7 + sw 8 also fail to halt the collapse, the MPM engine fork (`dicty_engine_mpm.py` already in repo) becomes load-bearing as the B27 plan.

- 2026-06-17: Batch 24 read-back complete; no pending instructions at read time. **B24 PRODUCED TWO DECISIVE FINDINGS AND ONE META-FAILURE.** 256 sims at the B24 parent (= B23 parent + pulse_dens DROPPED from schedule, kept in code; NO parameter change). **(1) ENGINE-RESOLUTION PROBES DECISIVELY NEGATIVE (Est #81):** TWO independent camp.res sweeps (sw 5 parent, sw 15 at c_sat=0.30 densification joint) show mound count INVARIANT across grid resolution in [112, 360]; the dt sweep (sw 7) shows mound count INVARIANT across working dt bands; coarse res<=96 falls to 1-2 spots from grid undersampling; res=400 catastrophic at BOTH joints (numerical aliasing single-replica). The 5-7 mound ceiling is NOT engine-resolution-bound or dt-bound. **(2) SMOKING GUN — RUNAWAY COMPACTION (Est #82):** TWO independent n_frames sweeps (sw 6 parent, sw 14 densification) BOTH show monotone-UP loss (0.94 → 1.26) and monotone-UP inner_mass (0.32 → 0.83) as n_frames extends from 200 to 1600. Morphology: 2-3 distinct mounds at n_frames=200-480, gradual coalescence to a SINGLE TINY POINT by n_frames=1000+. THE POINT-CELL ENGINE WITH THE CURRENT OPERATOR FAMILY HAS NO STABLE MULTI-MOUND ATTRACTOR; multi-mound exists only as a TRANSIENT because the simulation is *evaluated* at frame 400 before the next collapse phase. Refines Est #31 from "equilibrates by frame 400" to "the model is evaluated at frame 400 in the multi-mound transient phase". This is the strongest mechanistic motivation for engine-side intervention yet identified. The missing biological constraint is plausibly FINITE CELL VOLUME (mature mounds should saturate adhesion; finite-volume short-range repulsion balances). **(3) META-FAILURE — morph_score NOT IMPLEMENTED in B24.** The B24 plan promised metric augmentation (`eval_sweeps.py` to compute and report `morph_score = w_peak * peak_count_match + w_dens * per_spot_density_match` alongside `loss`) but the implementation was missing — sweep_results.json contains only `loss` and `inner_mass`. The CENTRAL B24 fork question (is the parameter-surface flatness metric-induced?) is UNADJUDICATED in this batch. **PLATEAU EXTENSIONS confirmed:** sense_sat.gain plateau extended to 7000 (sw 1, Est #54), c_sat ridge extended to 1.2 (sw 2, Est #57), sat_n productive plateau at c_sat=0.30 = [1.5, 2.7] (sw 3/8, Est #83), kadh plateau holds at densification joint (sw 11, Est #59), dt aliasing landscape reconfirmed (Est #9/#69). **NEW MORPHOLOGY CANDIDATE (Est #85):** sw 10 r_on=0.23 at c_sat=0.30, gain=1500 produces visible **4-MOUND** morphology — the MOST multi-mound configuration of B24, statistically tied with parent on loss (0.9173 vs 0.9126). Would-be morph_score winner if metric had been implemented. **NEW Est #84:** cell.n>=3800 at sparse-densification joint introduces an UNRECRUITED HALO (catchment radius finite, loss climbs to 1.09 at n=6000). Engine SOLVENT to n=6000. **RW silent across 9 batches now** — permanently dropped. NO new project best (sw 0 seed=0 = 0.9126 tied multiple times). **B25 STRATEGIC FORK — DOUBLE-DELIVERABLE BATCH:** (alpha) IMPLEMENTED morph_score in `eval_sweeps.py` this batch (n_mounds, peak_frac, morph_score now reported per value; ranks sweeps by both metrics; adjudicates Est #42 SSIM/morphology divergence flag live since B14); (beta) NEW OPERATOR `density_repel` added to `dicty_ops.py` (density-saturating short-range repulsion via a tanh gate of local rho_norm; pushes cells down-gradient when rho exceeds thr; biologically: finite cell volume). DECISIVE B25 sw 7: density_repel x n_frames=1200 — if the operator HALTS the runaway compaction (Est #82), inner_mass stays low at extended integration and the missing mechanism is identified. B25 PARENT = B24 parent + density_repel at strength=0 (ablation; non-zero introduced via sweeps). **Flag for user:** if density_repel FAILS the necessity + 5 joints + n_frames=1200 break test + high-strength seed sweep (11th project mechanism falsified, 7th cell-side), the engine-side intervention family is also exhausted and B26 escalates to the FULL ENGINE FORK (`dicty_engine_mpm.py` — soft-particle MPM with native finite-volume cells; prototype already exists in repo with `mpm_sweep_*` artefacts). The morph_score implementation is preserved across the fork for adjudication continuity.

- 2026-06-17: Batch 23 read-back complete; no pending instructions at read time. **B23 DECISIVELY FALSIFIES pulse_dens — NINTH project mechanism / FIFTH field-side mechanism (after B5 pacemaker, B9 inhibitor, B20 decay_dens, B22 diff_dens).** 256 sims at the B23 parent (secrete=11 revert recovered Est #73 σ≈0.04 tighter noise floor; pulse_dens added to schedule with amplitude=0 ablation default). Across SEVEN amplitude sweeps (sw 1 necessity at parent; sw 3-7 at five productive joints — c_sat=0.30, sense_sat.gain=1500, cell.n=2500, spring.kadh=20, relay.thr=0.30) PLUS sw 2 threshold sweep at amp=1.0 PLUS sw 8 high-amplitude seed sweep at amp=2.0, ablation (amplitude=0) wins or ties at every joint; every amplitude≥0.05 produces MONOTONE-UP loss (0.913→1.04 at amp=0.1, 1.14 at amp=2.0, 1.23 at amp=32, 12.8 at amp=60). Threshold sweep (sw 2) U-shaped: thr in [1, 5] catastrophic (pulse fires constantly), thr in [7, 15] parent-tied (pulse never fires = effective ablation). NO productive interior threshold. The 16 seeds at amplitude=2.0 (sw 8) all in [1.140, 1.200], DISJOINT from sw 0 ablation [0.91, 1.00] — confirms catastrophe is DETERMINISTIC, not seed-luck (same evidence pattern as B22 sw 10/11, B20 sw 14, B19 sw 14). FAILURE MODE: the local cAMP pulse PUSHES cells AWAY from dense regions (as designed) but they DO NOT NUCLEATE new mounds — they just disperse. Same family as decay_dens (Est #68) and diff_dens (Est #78); they all destroy existing aggregation without seeding new spatial structure. Promoted to **Est #80**. PLATEAU REFINEMENTS reconfirm B23 parent under tighter noise floor: sw 9 sat_n=2.1 tied at 0.9126; sw 10 gain plateau [300, 3500] no peak (Est #50 retraction permanent; Est #54 reconfirmed); sw 11 vmax=0.061 with aliasing wall reconfirmed at vmax≥0.075 (Est #66); sw 12 camp.D working band [0.0001, 0.035] with ringy near-failures at D=0.036/0.042 (Est #65/#76); sw 13 c_sat ridge plateau [0.20, 1.50] reconfirmed (Est #49/#57/#59); sw 14 relay.gain=140 plateau (Est #67/#74); sw 15 inflow.rate=4 plateau (Est #56/#59). NO new project best — multiple ties at 0.9126. **OPERATOR-SIDE MECHANISM FAMILY DEFINITIVELY EXHAUSTED.** 10 mechanisms falsified across 23 batches (5 field-side + 6 cell-side); sense_sat (B13) the ONLY structural addition that succeeded — as a REGULARIZER enabling the dense multi-mound regime, but not lifting the 5-7 mound count ceiling vs REAL=8. The structural ceiling is a property of the engine + metric pair. **B24 STRATEGIC FORK — METRIC AUGMENTATION first (cheaper, reversible), engine change second.** B24 plan: (a) edit `eval_sweeps.py` to compute and report a secondary `morph_score = w_peak * peak_count_match + w_dens * per_spot_density_match` alongside the existing loss, RANKING configurations by both; (b) sweep design focuses on the c_sat=0.30 SPARSE densification column (Est #53 axis — the strongest known densification handle, but never systematically re-evaluated under morph_score), the (c_sat, sat_n, gain) ridge, cell.n WIDE [400, 6000] via buffer extension, and FOUR ENGINE-RESOLUTION PROBES (camp.res sw 6/16, n_frames sw 7/15, dt sw 8) that have never been systematically tested in 23 batches. The decisive fork: if morph_score has a SHARP interior optimum where loss is FLAT, the parameter-surface flatness is metric-induced and B25 promotes the morphology winner. If morph_score is ALSO flat, that DEFINITIVELY indicts the engine and B25+ forks to `dicty_engine_mpm` (the MPM prototype already exists in the repo with `mpm_sweep_*` artefacts; introduces continuum deformable cells + density-dependent rheology). **Flag for user (Est #42, live since B14):** B24 introduces the morph_score AS A SECONDARY METRIC, preserving the existing loss for cross-batch comparison. If you prefer that the LOSS itself be augmented (so optimization is steered by morphology, not SSIM), please indicate; the current B24 plan keeps them separate to maintain auditable continuity with B1-B23.
- 2026-06-17: Batch 22 read-back complete; no pending instructions at read time. **B22 DECISIVELY FALSIFIES diff_dens — FOURTH FIELD-SIDE MECHANISM (EIGHTH project mechanism, FOURTH field-side after B5 pacemaker, B9 inhibitor, B20 decay_dens).** 256 sims at the B21 parent + `dicty_ops.py:918` patched (D0 now reads `fld.D`; B21 silent-operator bug Est #72 fixed). Across NINE sweeps testing kappa (sw 1 necessity at parent, sw 2-6, 8, 9 at seven productive joints: c_sat=0.30, gain=1500, kadh=20, relay.thr=0.30, cell.n=2500, inflow=1.5, sat_n=2.5), ablation (kappa=0) wins; every kappa≥0.05 produces a CATASTROPHIC loss jump (0.91→1.24-1.40) and morphology collapses to sparse-scatter (gradient annihilated, same failure mode as Est #68 decay_dens). Sw 10 (16 seeds at kappa=2.0) and sw 11 (16 seeds at kappa=20) DECISIVELY confirm the catastrophe is deterministic, not seed-luck — median 1.27 vs ablation 0.99; distributions disjoint. EXCEPTION sw 7 (kappa × camp.D=0.005): the only joint where kappa>0 helps, but only because D=0.005 itself is over-diffusive; best rescued loss 1.030 at kappa=0.8 is still +12% above parent 0.911 (NEW Est #79 — self-cancelling pair, not a mechanism). **Promoted to Est #78.** Plateau refinements re-confirm parent: sw 12 sat_n=2.1 plateau-tied at 0.9111; sw 13 secrete.rate=9 tied with rate=11 at 0.9111 (motivates B23 revert to rate=11 for Est #73 tighter σ≈0.04 noise floor); sw 14 vmax=0.060 aliasing wall reconfirmed at vmax≥0.075; sw 15 camp.D wall transition is RINGY in [0.038, 0.055] with anomalous dip D=0.046 (refines Est #76 to working band [0.0001, 0.036]). NO new project best — multiple ties at 0.9111. Parameter surface fully exhausted; operator-side mechanism well is dry: 4 field-side + 6 cell-side falsified. **B23 STRATEGIC FRAME:** scheduled NEW operator `pulse_dens` (density-triggered local cAMP burst — SOURCE-SIDE + DETERMINISTIC + LOCAL + DENSITY-CONDITIONED, filling the unexplored quadrant). Distinct from FALSIFIED random Poisson nucleation B6 (stochastic, density-independent), homogeneous pacemaker B5 (global + time-periodic), inhibitor B9 (separate dispersive field), decay_dens B20 (sink-side, mass-shrinking), diff_dens B22 (transport-side, field-annihilating). Implementation: c(x) += amplitude * sigmoid((rho_norm(x)-thr)/width) * dt; refractory provided implicitly by camp.decay+diffuse+sense_sat homeostasis. **B23 PLAN:** (1) sw 0 seed sweep at amplitude=0 validates patch + secrete=11 revert + recovers σ≈0.04 floor; (2) sw 1 amplitude necessity+sufficiency [0, 60] DECISIVE; (3) sw 2 threshold sweep at amp=1.0 (does thr matter or universal catastrophe?); (4) sw 3-5 amplitude JOINTS with three densification axes (c_sat=0.30, gain=1500, cell.n=2500); (5) sw 6-7 JOINTS with morphology candidates (kadh=20 B21 winner, relay.thr=0.30 Est #33); (6) sw 8 high-amplitude seed sweep at amp=2.0 DECISIVE distinguishes productive from catastrophe; (7) sw 9-15 plateau re-pins under secrete=11 (sat_n FINE, sense_sat.gain WIDE re-resolve Est #50 vs #54, vmax FINE, camp.D wall ladder, c_sat ridge re-pin, relay.gain FINE, inflow FINE). DROPS: diff_dens.*, decay_dens.*, secrete_het.*, r0, persistence, sense_adapt, align, inhib, nucleation, inflow.bias/edge_band, pacemaker omega, random_walk, n_frames, relay.eps, secrete.rate FINE (handled by revert), kadh wide. **Flag for user:** if `pulse_dens` ALSO fails the necessity+sufficiency + 5 joints + high-amp seed sweep, the operator-side mechanism family is DEFINITIVELY EXHAUSTED and B24 escalates to ENGINE CHANGE (3D, soft particles, finer grid) or METRIC AUGMENTATION (Est #42 SSIM/morphology divergence — explicit peak-detection gate or per-spot density term added to the loss). The Est #42 flag has been live since B14 without action. B24 may need to address it directly rather than continue adding operators within the current 2D point-cell engine.
- 2026-06-17: Batch 21 read-back complete; no pending instructions at read time. **B21 INVALID for diff_dens adjudication — implementation bug discovered.** All 6 diff_dens kappa sweeps (sw 1, 2, 3, 5, 10, 11, 15) returned BIT-IDENTICAL losses across all 16 kappa values [0, 120] at every joint. Root cause: `DiffDens.forward` in `dicty_ops.py:918` reads `getattr(fld, "diffusion", 0.0)` to compute D0 for the Laplacian-correction step, but `GridField` stores the diffusion constant as `self.D` (`grid_field.py:31`). Hence D0=0 for every kappa → operator subtracts 0 from camp.grid → no-op regardless of kappa. **PATCH applied** to `dicty_ops.py:918`: fallback `getattr(fld, "D", getattr(fld, "diffusion", 0.0))`. Mechanism NOT adjudicated; new Est #72 records the bug; no entry in Established/Falsified for diff_dens (re-test in B22). **Net B21:** 6/16 slots wasted on silent diff_dens, 1/16 wasted on duplicate sw 13 (= sw 0 seed sweep), 9/16 informative — all reconfirm parent loss 0.9111 (unchanged from B20) with minor refinements: Est #73 (secrete=9 noise floor σ≈0.11 ~2.5× wider than secrete=11), Est #74 (relay.gain plateau extended to 320), Est #75 (sat_n plateau extended to [1.7, 3.2] at B21 parent), Est #76 (camp.D wall transition starts at D=0.045, working band tightens to [0.0001, 0.035]), Est #77 (secrete safe-band lower edge tightened to rate=9). **SIDE-FINDINGS from silent diff_dens sweeps at fixed joints:** sw 2 (c_sat=0.30) → 5-mound at 0.97; sw 3 (gain=1500) → 5-6 multi-mound at 1.10 (Est #53 reconfirmed); sw 10 (relay.thr=0.30) → 5-mound at 0.97 (Est #33 sparse-multi candidate closer to parent now); sw 11 (kadh=20) → **visually BEST 5-6 crisp multi-mound of batch** at 1.25 (SSIM-penalised, Est #42). **B22 plan:** 11 slots on FIXED diff_dens × maximally-diverse joint configurations (c_sat=0.30, gain=1500, kadh=20, relay.thr=0.30, cell.n=2500, camp.D=0.005, sat_n=2.5, inflow=1.5 + 2 seed sweeps at kappa=2 and kappa=20) + 5 plateau-refinement slots (sat_n FINE narrow around 2.1, secrete.rate FINE around 9 with low-end resolution, vmax avoid-resonance set, camp.D wall-edge ladder, kappa × kadh=20 because of sw 11 morphology winner). DROP duplicate seed sweep. **Flag for user:** if FIXED diff_dens ALSO silent (all kappa losses bit-identical even with D0 wired), the operator's Laplacian-correction approach itself is wrong (e.g., grid boundary conditions mismatch); a 2nd-order rewrite using the actual `fld.D` modulation would be needed. If FIXED diff_dens is non-silent but FLAT (parent-tied within noise across all 16 kappa) at the necessity test + all 11 joints, then diff_dens becomes the FOURTH falsified field-side mechanism (after B5 relay-pacemaker, B9 inhibitor, B20 decay_dens) and B23 escalates to DENSITY-TRIGGERED PULSE (deterministic local burst when ρ>θ — different from FALSIFIED random Poisson nucleation B6 and homogeneous pacemaker B5). Beyond B23, the structural-ceiling diagnosis becomes load-bearing: the 2D periodic point-cell model with the current operator family may fundamentally limit multi-mound count, and the next escalation is an ENGINE CHANGE (3D, soft particles, finer grid) or a METRIC AUGMENTATION (Est #42 SSIM/morphology divergence flag remains live — kadh=20 sw 11 was visually-best in B21 at loss=+0.34 above parent, exemplifying the bias).
- 2026-06-17: Batch 20 read-back complete; no pending instructions at read time. **B20 DECAY_DENS FALSIFIED — SEVENTH project mechanism falsified (THIRD field-side).** 256 sims at the B19 parent + new `decay_dens` operator (ablation dens_coeff=0). Across SIX sweeps testing dens_coeff (sw 1, 2, 3, 5, 10, 11) at every productive joint (parent, c_sat=0.30, gain=1500, cell.n=2500, camp.decay=0.20, kadh=20), ablation wins or ties; coeff≥1.2 produces UNIVERSAL field annihilation — cAMP gradient destroyed, cells stall, mounds dissolve to sparse-scatter or near-empty FOV (inner_mass collapses to 0.004 by coeff=5). Sw 14 (seed sweep at coeff=1.0) gives loss distribution statistically tied with ablation but with visibly sparser per-mound morphology — degraded, not productive. Sw 15 (sat_n × coeff=1.0) confirms no sat_n rescues. NO config crosses 7 mounds. **Promoted to Est #68.** Field-side mechanism family now has 3/3 candidates falsified: pulsatile relay (B5), inhibitor (B9), decay_dens (B20). **NEW project best loss = 0.9111 at secrete.rate=9** (sw 8; was B19 0.9126) — marginal within seed noise; conservative B21 parent adoption (Est #43). **NEW Est #69** (vmax aliasing landscape under sat_n=2.1 BROADER than under sat_n=3.0; multiple resonances at {0.054, 0.065, 0.068, 0.071, 0.0735, 0.0745, 0.0748} interleaved with working dips {0.058, 0.061, 0.072, 0.0743}). **NEW Est #70** (sat_n=2.1 plateau is BROAD across [1.6, 2.7]; refines #61/#64 width). **NEW Est #71** (cell.n engine-solvent across [800, 3400] with NO capacity wall at sat_n=2.1; reconfirms Est #52 for 5th batch and removes capacity-wall caveat). **B21 NEW MECHANISM: `diff_dens`** — second field-side candidate (decay_dens failure mode was field annihilation; diff_dens MODULATES TRANSPORT and conserves field mass). D_eff(x) = D0/(1 + kappa*rho_norm(x)) — diffusion SUPPRESSED in dense regions, sharpening boundary gradients. Ablation kappa=0 = parent. Biological rationale: dense mound interior geometrically slows cAMP transport through reduced inter-cellular volume. Distinct from FALSIFIED inhibitor (separate field), pulsatile relay (homogeneous pacemaker), random nucleation (Poisson pulse), decay_dens (annihilates field). **B21 plan:** (1) sw 0 seed sweep at kappa=0 validates new operator + secrete=9 conservative change; (2) sw 1 kappa necessity+sufficiency [0, 120] — decisive ceiling-breaking test; (3) sw 2-3, 6 kappa JOINTS with three densification axes (c_sat=0.30, gain=1500, cell.n=2500); (4) sw 4 sat_n FINE around 2.1; (5) sw 14 high-kappa seed sweep (analogue of B19/B20 sw 14 — **DECISIVE: if kappa=2.0 distribution shifts LOWER than ablation, diff_dens is the first productive field-side mechanism**); (6) sw 7-10 plateau re-pins (relay.gain, vmax, secrete.rate, camp.diffusion at wall edge); (7) sw 11 kappa × relay.thr=0.30 (recovers Est #33 sparse-multi candidate?); (8) sw 12 kappa × kadh=20; (9) sw 13 cell.n FINE re-confirm Est #71; (10) sw 15 sat_n × kappa=2.0 joint; (11) sw 16 kappa × camp.diffusion=0.005 (elevated base D makes modulation more visible). **Flag for user:** if diff_dens ALSO fails the necessity+sufficiency + 4 joints, the next B22 candidate is DENSITY-TRIGGERED PULSE (deterministic local burst — different from random Poisson nucleation B6 and homogeneous pacemaker B5). Beyond B22 the structural-ceiling diagnosis becomes load-bearing: the 2D periodic point-cell model with current operator family may fundamentally limit multi-mound count, and the next escalation is an ENGINE CHANGE (3D, soft particles, finer grid) or a METRIC AUGMENTATION (the Est #42 SSIM/morphology divergence flag remains live; sw 8 rate=9 sharp spike at rate=10 suggests SSIM still has single-replica fragility). Note on log append: dicty_loop_log.md append via `python3 append_log.py` was BLOCKED at runtime; B20 per-sweep entries are written to `_b20_log.md` (canonical artefact matching `_b18_log.md`, `_b19_log.md` convention) and a one-line marker added to the main log.
- 2026-06-17: Batch 19 read-back complete; no pending instructions at read time. **B19 SECRETE_HET FALSIFIED — SIXTH cell-side mechanism falsified.** 256 sims at the B18 parent + new `secrete_het` operator (ablation het_std=0). Across SEVEN sweeps testing het_std (sw 1, 2, 3, 4, 10, 11, 15) at every productive joint (parent, c_sat=0.30, gain=1500, cell.n=3000, kadh=20, r_on=0.215, relay.gain=300), ablation wins or ties; het_std>=0.7 monotone-up loss to ~1.18 and DISPERSES per-mound density (inner_mass collapse 0.35->0.15 by het=2.5). NO config crosses 7 mounds toward 8. Sw 14 (seed sweep at het_std=1.0) confirms ALL 16 seeds elevated [1.03, 1.11] with visibly sparser per-mound morphology. **Promoted to Est #63.** Cell-side mechanism family now EXHAUSTED after nucleation (B6), sense_adapt (B7), align (B8), inhibitor (B9), persistence (B10/B11), secrete_het (B19). **NEW project best loss = 0.9126 at sat_n=2.1** (sw 5; was B18 0.9167) — single conservative B20 parent adoption (Est #43). **NEW Est #64** (sat_n=2.1 at c_sat=0.50 project-best, refines Est #61 plateau center). **NEW Est #65** (camp.diffusion catastrophe wall at D~0.07, not D~0.05 as B18 estimated; D=0.05 is transition midpoint). **NEW Est #66** (vmax aliasing wall refined upward to >=0.075; working band [0.054, 0.074]). **NEW Est #67** (relay.gain productive plateau [100, 280]; monotone-up degradation past 320). Engine no longer NaN at cell.n=3400 (B18 buffer fix; capacity now intermittent spikes). **B20 NEW MECHANISM: `decay_dens`** — first FIELD-SIDE structural mechanism after cell-side exhaustion. Per-mound density-coupled cAMP decay: `c <- c - dens_coeff * rho_norm * c * dt` where `rho_norm` is mean-normalised local cell density on the camp grid. Ablation dens_coeff=0 = parent. Biological rationale: PDE phosphodiesterase upregulated in dense regions accelerates local cAMP turnover, capping per-mound size and allowing distal nucleation. Distinct family from FALSIFIED inhibitor (B9, separate field with dispersive dynamics), pulsatile relay (B5, homogeneous pacemaker), random nucleation (B6, Poisson pulse). decay_dens is LOCAL, DETERMINISTIC, and only modulates the existing camp turnover rate. **B20 plan:** (1) sw 0 seed sweep at dens_coeff=0 validates the operator + sat_n=2.1 conservative change; (2) sw 1 dens_coeff necessity+sufficiency [0, 40] -- decisive ceiling-breaking test; (3) sw 2-3, 6 dens_coeff JOINTS with the three densification axes (c_sat=0.30, gain=1500, cell.n=2500); (4) sw 4 sat_n FINE around 2.1; (5) sw 14 high-dens_coeff seed sweep (analogue of B19 sw 14 high-het) -- distinguishes productive from degraded regime; (6) sw 6-11 plateau re-pins (relay.gain, vmax, secrete.rate, camp.diffusion); (7) sw 11-12 dens_coeff joints with kadh/decay; (8) sw 12 c_sat ridge re-pin; (9) sw 15 sat_n × dens_coeff=1.0 joint. **Flag for user:** if decay_dens FAILS across the necessity+sufficiency + 4 joints, the next B21 candidate is DENSITY-MODULATED DIFFUSION (next-most-mechanistically-distinct field-side mechanism). Beyond that, the structural-ceiling diagnosis becomes load-bearing: the 2D periodic point-cell model may fundamentally limit multi-mound count given the catchment-area scaling, and the next escalation is a higher-fidelity engine (3D, soft particles, or a metric augmentation). The Est #42 SSIM/morphology divergence flag remains live but unactioned since B17.
- 2026-06-17: Batch 18 read-back complete; no pending instructions at read time. **B18 STRUCTURAL CEILING CONFIRMED — parameter densification has plateaued.** 256 sims at the cell.n=1800 single-axis parent (only conservative change from B17 per Est #43) produce 4-7 mound morphology at every sweep × value combination; NO 8-mound configuration found despite extreme pushes (sense_sat.gain to 7000 sw 13/14, c_sat to 1.2 sw 2, sat_n through ridge sw 3). Best loss=0.9167 at cell.n=3200 (sw 9), marginal vs B17 best=0.9268 (within seed noise sigma=0.04). **DRAMATIC failure-mode dismantling continued (Est #59):** camp.decay plateau [0.04, 2.0+] (sw 7, 28x parent still aggregates), inflow.rate [0, 150] (sw 8, 37x parent), spring.kadh [6, 280] (sw 4). **NEW WALL DISCOVERED (Est #60):** camp.diffusion CATASTROPHIC at D>=0.05 (sw 15 loss 1.85→5.77); the B16 sw 11 "flat to 0.08" missed this wall. **Est #61:** sat_n=3.0 (B16 parent) sits at the UPPER EDGE of the productive plateau; sat_n in [1.9, 2.5] is the productive band. **Est #62:** secrete.rate degrades morphology monotonically past rate=14; safe band [8, 14]. **B19 ACTION:** parameter densification at c_sat=0.50 column is exhausted. Adopted the strongest open-Q hypothesis: ADDED NEW MECHANISM `secrete_het` (per-cell heterogeneous secretion via log-normal multiplier, ablation = het_std=0; biological rationale = Dicty cells not synchronous in aggregation competence). Distinct family from FALSIFIED per-cell gain (sense_adapt), polarity (align, persistence), and field-side mechanisms (nucleation, inhibitor, pacemaker). B19 plan: (1) sw 0 seed sweep at het_std=0 validates new op preserves Est #48; (2) sw 1 het_std necessity+sufficiency [0, 2.5] with ablation; (3) sw 2-4 het_std JOINTS with the three densification axes (c_sat=0.30, gain=1500, cell.n=3000) — DECISIVE test of mound-count breakthrough; (4) sw 5 sat_n FINE refinement in productive band; (5) sw 6 camp.diffusion FINE around D=0.05 wall; (6) sw 7 secrete.rate FINE in safe band; (7) sw 8 vmax FINE avoiding 0.063-0.064 resonance; (8) sw 9 het_seed sweep at het_std=0.5; (9) sw 10-11 het × kadh/r_on joints; (10) sw 12 cell.n FINE at capacity edge; (11) sw 13 relay.gain FINE; (12) sw 14 high-het seed sweep; (13) sw 15 het × relay.gain=300 joint. **Flag for user:** if het_std produces no mound-count breakthrough across the 5 sweeps testing it (necessity + 4 joints), the next B20 candidate is **density-coupled cAMP decay** (over-populated mounds decay faster → ceiling on per-mound size → multi-mound nucleation). Beyond that, the central question becomes whether the model's domain (2D, periodic, point cells) fundamentally limits multi-mound count, or whether the catchment-area scaling is the binding constraint.
- 2026-06-17: Batch 17 read-back complete; no pending instructions at read time. **B17 NEW PROJECT BEST LOSS UNDER SSIM = 0.9268 (sw 0 seed=8)** at the 3-axis-adopted joint parent (gain=500, secrete=11, kadh=10 on top of sat_n=3.0, c_sat=0.50). seed sweep (sw 0) confirms Est #48 reproducibility holds under joint adoption (all 16 seeds → 5-7 dense compact mounds, sigma=0.04). **MAJOR FINDING — sw 13 DENSIFICATION AXIS:** sense_sat.gain × c_sat=0.30 is MONOTONE-DOWN from gain=100 (1.13) to gain=1000 (0.9823) with visible densification AND apparent increase in mound count toward 7-8 — promoted to Est #53, the first axis-level mechanism signal below parent loss since B16. **Est #50 RETRACTED** (gain=500 peak with reversal at 600+ was REGIME-SPECIFIC to old parent; sw 1 flat under joint adoption); promoted to Est #54. **NEW Est #55** (sense_sat extends regularization to relay catastrophe band — ringing at gain ∈ [30, 60] dismantled, only gain<30 catastrophic). **NEW Est #56** (failure-mode map nearly fully dismantled: inflow flat to 40, decay flat to 0.85, secrete morphology-safe to 14, kadh plateau to 240; only vmax aliasing persists per Est #9/#51). **NEW Est #57** (ridge quantified: passes through (0.30, 2.0), (0.50, 3.0), (1.0, 3.5), (2.5, 4.0)). **B18 PARENT:** single conservative change cell.n: 1000 → 1800 (sw 10 marginal dip, multi-mound preserved). B18 plan: 16 sweeps focused on (1) PUSHING the densification axis (sw 2 gain × c_sat=0.30 to 5000, sw 14 gain × c_sat=0.20 to 7000, sw 15 gain × c_sat=0.40 to 5000) to test if mound-count ceiling breaks toward REAL=8; (2) full alternative-parent ablation grid (sw 3 c_sat at gain=1500); (3) joint re-tests of kadh/r_on/relay/cell.n inside the densification regime; (4) camp.diffusion at c_sat=0.30 as direct mound-wavelength probe (sw 16); (5) extended failure modes (decay to 2.0, inflow to 150); (6) vmax fine avoiding the 0.065 resonance. **Flag for user (recurrence of Est #42):** the SSIM/morphology divergence reduced — best loss (0.9268) is at morphology-stable parent; SSIM-artifact secondary minimum (sw 3 secrete=50 = 0.976) is now clearly above. The metric may finally be aligned with morphology, but the structural mound-count ceiling (5-7 vs REAL=8) remains. If B18 densification plateaus before 8 mounds, the next B19 mechanism candidate is **per-cell secretion heterogeneity** (untested; B7 per-cell desensitization falsified the gain-modulation variant, but a per-cell secretion-rate distribution is structurally different).
- 2026-06-17: Batch 16 read-back complete; no pending instructions at read time. **B16 BREAKTHROUGH — first morphologically robust parent in 16 batches, first genuine best-loss under SSIM.** B16 sw 0 (seed) shows the B15 sw 7 winner config (sat_n=3.0, c_sat=0.50) reproduces clean 5-7 distinct dense compact mounds at ALL 16 seeds, sigma_loss=0.04 (lowest noise floor measured); validates Est #43 lesson (adopt WHOLE-CONFIG morphology winner). B16 sw 5 (sense_sat.gain) found a true peak at gain=500 with reversal beyond 600 (over-attraction collapses multi-mound to single blob) — **loss=0.9802 inner=0.372 is the BEST PROJECT LOSS under the SSIM metric**. New Est #48-52: parent reproducibility (#48), (c_sat, sat_n) trade-off ridge (#49), gain=500 peak with reversal (#50), sense_sat regularizes operator-level failures but NOT vmax aliasing (#51), cell.n still not a densifier (#52). DECISIVE TESTS RESOLVED: sw 1 confirms Est #4 (relay necessary at sat_n=3.0) and FALSIFIES Est #47 (the B15 relay destruction was a broken-parent artifact). B17 plan: 16 sweeps focused on (1) seed-sweep validation of multi-axis adoption [gain=500, secrete=11, kadh=10], (2) refinement of gain/c_sat/sat_n peaks, (3) MAPPING THE (c_sat, sat_n) ridge (sw 12, 13), (4) DENSIFICATION joints (gain x c_sat=0.30 sw 14, gain x kadh=6 sw 15), (5) extended failure-mode boundaries (camp.decay to 0.85, inflow to 40), (6) cell.n at high inflow joint. **Flag for user (Est #51 nuance):** sense_sat regularizes most operator failure modes but NOT vmax aliasing — at sat_n=3.0 the Est #9 aliasing wall returns at vmax>=0.072. This is an integration-step phenomenon (camp.res=160 grid, dt=0.5), not a chemotaxis dynamics issue. **Flag for user (the prior Est #42 SSIM/morphology divergence):** B16 produces a config with clear visual multi-mound morphology AND best-of-project loss (0.98) — the SSIM bias is no longer dominant. The remaining residual is per-mound DENSIFICATION (SIM~5-7 mounds with sparser per-mound density vs REAL~8 mounds densely packed). Open: should we still augment the metric with an explicit mound-detection gate, or is the SSIM signal now sufficient given the morphological convergence?
- 2026-06-17: Batch 15 read-back complete; no pending instructions at read time. **B15 CATASTROPHIC PARENT FAILURE — methodological lesson.** The B14 "science-over-loss" parent (joint adoption of single-axis B14 winners: sat_n=1.25, c_sat=0.20, gain=240, kadh=15, r_on=0.20, decay=0.07, inflow=4) collapsed to dispersion at loss=11.78 — 10x worse than B14 parent. 14 of 16 sweeps could not produce credible morphology at this broken parent. NEW Est #43: single-axis morphology peaks are NOT TRANSITIVE across joint changes; the next batch must adopt the morphology winner AS A WHOLE CONFIG rather than recombining "best of axis" findings. The genuine B15 breakthrough was sw 7 (sat_n=3.0 c_sat=0.50): clean 5-6 DENSE compact mounds, loss=1.10, the actual dense-multi regime — promoted to Est #45. NEW Est #44 (sat_n is the master regime switch; sat_n>=1.9 mandatory for aggregation under sense_sat); NEW Est #46 (kadh-sat_n coupling: low kadh wins at sat_n=2, high at sat_n=1.25); NEW Est #47 (relay was destructive at broken B15 parent — re-test at sat_n=3.0 in B16 is the decisive Est #4 experiment). B14 Est #38 (sat_n peak around 1.5), Est #39 (gain monotone-densifies), Est #40 (low kadh wins) all PARTIALLY RETRACTED as regime-dependent. New B16 parent = B15 sw 7 winner config: c_sat=0.50, sat_n=3.0, all else unchanged from B15. B16 plan: re-test every adopted axis (relay.gain ablation as DECISIVE Est #4 test, kadh, sat_n FINE, c_sat FINE, gain, r_on, inflow, decay, cell.n, secrete, camp.diffusion, relay.thr, vmax) at the new credible parent + two (sat_n x c_sat) joint sweeps + seed sweep. **Flag for user:** the SSIM/morphology divergence (Est #42) was QUANTIFIED at the B14 parent but the B15 demonstration showed the metric is also sensitive to which regime the parent sits in — at a broken parent the loss surface is uniformly bad and SSIM cannot adjudicate. Should the metric be augmented with an explicit aggregation/mound-detection gate so that "broken-parent" runs are flagged separately from "morphologically-credible-but-imperfect" runs?
- 2026-06-17: Batch 14 read-back complete; no pending instructions at read time. **B14 SCIENCE-OVER-LOSS DECISION + SSIM BIAS QUANTIFIED.** The B13 c_sat=0.20 "breakthrough" was in the SPARSE-multi-spot regime; B14 mapped the densification surface and found (a) c_sat=0.4-1.0 = DENSE multi-mound (Est #38), (b) sense_sat.gain monotone-densifies up to 240 (Est #39 — the first per-parameter densification lever), (c) low spring.kadh=5-20 outperforms high (Est #40), (d) saturation broadens field-side parameter bands (Est #41 — partial retraction of Est #5 + #9), (e) the SSIM loss is structurally BIASED against multi-mound by ~25% (loss-winner sat_n=0.5 diffuse vs morphology-winner sat_n=1.25 dense-multi — Est #42). New B15 parent = sat_n=1.25, c_sat=0.20, gain=240, kadh=15, r_on=0.20, decay=0.07, inflow=4 (the morphology winner, NOT the loss winner). B15 plan: 16 sweeps focused on (gain to 600 saturation, c_sat in [0.15, 2.0] sparse/dense boundary, sat_n peak in [1.1, 1.25], gain × c_sat=0.5 joint, r_on fine, kadh low-end, seed sweep for parent-seed selection). **Flag for user (Est #42):** the SSIM/morphology divergence is now quantified — should the loss weighting be revisited (e.g., add an explicit multi-mound-density term or down-weight SSIM relative to g(r))?
- 2026-06-17: Batch 13 read-back complete; no pending instructions at read time. **B13 BREAKTHROUGH** — `sense_sat` (Hill-saturated chemotaxis, sixth structural mechanism added) is the FIRST in 13 batches to break the single-blob/2-knot ceiling under SSIM. Sw 1 at c_sat=0.2 produces 5-6 distinct compact mounds with inner_mass=0.662 vs REAL=0.606 — closest visual match to REAL the model has produced. Loss does not yet reward (SSIM penalizes sparse spots); densification is the new frontier. Side-effects: sense_sat regularizes the secrete failure-mode (Est #29 explosive dispersion disappears) and renders relay non-necessary for multi-mound (partial retraction of Est #4). New Est #34-37, new B14 parent (c_sat=0.20, sat_n=2, r_on=0.225, vmax=0.061, r0=0.018). B14 plan: 16 sweeps focused on densification levers (kadh, r_on, sat_n joints with c_sat=0.20), clean relay ablation at the new parent, and a 2D c_sat × sat_n densification grid.
- 2026-06-17 (B10): **METRIC CORRECTED.** Acknowledged + acted on:
  * eval_sweeps.py confirms NEW loss = (1−SSIM_full-FOV-image, late-weighted) +
    0.2·g(r)-MSE + 0.3·mound-count-pen + 0.5·velocity-MSE.
  * The B10 sweep_results.json (read this batch) already uses the new metric:
    its `loss` values (0.27-0.86) are NOT comparable to B1-B9 (old radial-MSE).
  * inner_mass is now a DIAGNOSTIC ONLY (no longer optimized).
  * Ledger updated: new metric reference added; Est #2 (radial profile) flagged
    as superseded; Est #6/#7 now retired as "metric-specific artefact, n/a";
    Est #10 (single-blob gameability) becomes Est #10-old (the failure mode is
    now SUPPRESSED by the new metric, since SSIM-on-image rewards multi-mound
    in the right places).
  * Real n_mounds reported by `_real_fov_images` ≈ 8 (per instruction) — the
    new explicit morphological target (current model: 2-3 mounds — wider gap).
  * Batch 11 plan reframed around: raise SSIM (best B10 ~0.72; raise toward 1)
    and match n_mounds=8 (currently 2-3 in our SIM strips).

## (Old) Acknowledged
  - The old claim "use the angularly-averaged radial concentration profile (and inner-mass)" is
    **WRONG**: COM-recentering + angular averaging is a degenerate invariant that COLLAPSES the
    multi-mound structure (cannot tell one central blob from N mounds in a ring) — this is the root
    of your own Established #10 (inner_mass gameable by an over-tight single blob).
  - **New loss (eval_sweeps.run_one):** primary = late-weighted **(1 − SSIM of the full-FOV cell
    IMAGE)** (sim cells vs real movie cells, NOT COM-recentered) + 0.2·**g(r)** autocorrelation MSE
    (translation+rotation-invariant, structure-preserving) + 0.3·**mound-count** penalty + 0.5·velocity.
  - **Why SSIM-on-image is sound:** the sim is SEEDED from the real frame-0 positions (`init_npz`),
    so mound LOCATIONS should be reproducible — a correct model grows mounds WHERE the data does, so
    position-sensitive image SSIM is valid. (SSIM was only invalid on the COM-*recentered* density map.)
  - **inner_mass / radial profile are now DIAGNOSTICS ONLY** — do not optimize them.
  - New per-config metrics in sweep_results.json: `ssim` (↑ better, 1=identical), `n_mounds`
    (target: real has **~8 mounds**), `inner` (diagnostic). Aim: raise SSIM, match n_mounds≈8.
  - **Loss is NOT comparable to previous batches.** Re-establish the best under the new metric; add a
    dated metric-change entry to the ledger so before/after is auditable. KEEP your relay / inhib /
    sense_adapt mechanism findings — only the *scoring* changed, not the mechanism evidence.

## Acknowledged

- 2026-06-17: Batch 12 read-back complete; no pending instructions at read time. B12 256 sims at new clean parent (no persistence, r_on=0.24, n=800, sense.gain=80, inflow=4): noise floor sigma=0.18 (broader than B11), single-blob morphology at every parameter axis (the 2-knot B11 morphology lost at inflow>=4); five sharp failure-mode boundaries mapped (secrete in [3.6, 6.0]=dispersion; relay.gain in [10, 30]=ringing; relay.thr<0.15=overfire; camp.decay>0.4=field-dies; inflow.rate>6=over-dilution). One promising direction: relay.thr in [0.38, 0.50] shows visible 3-4 sparse mounds (sw 5). New Established #29-33 added. B13 parent: r_on=0.245, n=1000, vmax=0.062, inflow.rate=3.0 (small B12 improvements). NEW MECHANISM: `sense_sat` (Hill-saturated chemotaxis, ablation=c_sat=1e6) added to dicty_ops.py and base spec.
- 2026-06-17: Batch 11 read-back complete; no pending instructions at read time. B11 256 sims under SSIM-based metric confirm: loss surface is FLAT (~0.92-1.03, range within seed noise Δ=0.13) across all 16 single-axis sweeps; only spring.r_on shows a clean monotone morphological signal (r_on=0.24 → inner_mass=0.614 matches REAL 0.606); persistence is morphologically silent under inflow too (sw 0 falsified — closes the B10 decoupling question); inflow rate is flat-with-noise (Est #21 retracted); secrete.rate and camp.decay have new working-band boundaries identified. New B12 parent adopted: persistence DROPPED, r_on=0.24, n=800, sense.gain=80, inflow=4, secrete=7, decay=0.18.
- 2026-06-17: Batch 1 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 2 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 3 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 4 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 5 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 6 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 7 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 8 read-back complete; no pending instructions found at read time.
- 2026-06-17: Batch 9 read-back complete; no pending instructions found at read time.

