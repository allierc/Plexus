# Slots designed by the agent each batch.
# Format: name : SPEC specs/<file>.yaml [key val ...]  (dotted keys = op.param overrides)
#
# =====================================================================================================
# ===== CAMPAIGN REOPENED (2026-07-06) -- NEW TERMINUS STAGE **REG** (perturbation robustness). =====
# =====================================================================================================
#
# ORG is the LOCKED, established capstone (embryo_ORG_swap_anisoY_sed13.yaml, 2 coexisting growth programs,
# n=6) -- do NOT re-run it. The ladder now ends one rung later:
#   1A -> ... -> BUD -> BRN -> ORG -> REG, then STOP.
# current_stage.txt = REG. See user_input.md + instruction_embryo.md (REG stage def) for the mission.
#
# DESIGN THE FIRST REG BATCH (106) HERE: perturb the locked ORG organism mid-development (transient
# growth-program halt / mechanical insult / pattern-channel dropout -- primitives ONLY, no repair operator)
# and read whether the ORG signature RECOVERS (indep_domains->2, program_stability back above gate,
# fragment_count->1, pattern/branch restored) vs an unperturbed control AND a perturbed-but-frozen control.
#
# Overwrite this banner with your 8 slot lines (~4 exploit / 3 explore / 1 control).
