general:   # run-level settings
  name: tyssue_cell_morphogen_bulge   # run name (-> archive/<name>/)
  seed: 0   # RNG seed
  n_frames: 120   # number of simulation frames
  dt: 1.0   # time step (a label: the bounded-Euler relaxation is per-frame)
  boundary: free   # domain boundary (free = unbounded; border vertices are pinned)
  dim: 2   # spatial dimension
  world:   # world box size
  - 20.0
  - 22.0
sets:   # the biological SETS (Levels) of the model
  vertex:   # VERTEX set: mesh-vertex positions -- the mechanical DOF
    n: 503   # buffer size (max elements)
  cell:   # CELL set: per-cell biological state (the plexus2 two-level hierarchy)
    n: 222   # buffer size (max elements)
    state:   # per-element state blocks (name: {width, integration})
      a0:   # target cell area (biological state; drives shape_energy)
        width: 1   # number of scalar columns in this state block
      chem:
        width: 2   # number of scalar columns in this state block
      ctype:   # cell type / fate (0 = wild-type, 1 = clone)
        width: 1   # number of scalar columns in this state block
      area:   # cell area (aggregate readout from vertices)
        width: 1   # number of scalar columns in this state block
      perim:   # cell perimeter (aggregate readout)
        width: 1   # number of scalar columns in this state block
      cen:   # cell centroid (aggregate readout)
        width: 2   # number of scalar columns in this state block
fields: {}   # continuous fields
operators:   # the operators to instantiate (each realises one mechanism)
- op: mesh_seed   # build the honeycomb half-edge mesh on the vertex set (frame 0)
  at: vertex   # the set this operator acts on
  nx: 16   # lattice columns (cell centres)
  ny: 18   # lattice rows
  a: 1.0   # cell spacing (sets the base cell size)
  border: 1   # rings of border cells to drop (their vertices are pinned)
  jitter: 0.1   # initial positional disorder
  p0: 3.72   # target shape index -- the rigidity knob (< ~3.81 solid, > ~3.81 fluid)
  seed: 0   # RNG seed
  before_frame: 1   # run this operator once, before the given frame
- op: cell_seed   # init the cell set from the mesh: one live cell/face, a0=base, type=0
  at: cell   # the set this operator acts on
  a: 1.0   # cell spacing (sets the base cell size)
  before_frame: 1   # run this operator once, before the given frame
- op: cell_geometry   # AGGREGATE vertices -> per-cell area / perimeter / centroid (readouts)
  at: cell   # the set this operator acts on
- op: cell_morphogen
  at: cell   # the set this operator acts on
  amp: 1.0
  sigma: 1.5
  before_frame: 3   # run this operator once, before the given frame
- op: morphogen_growth
  at: cell   # the set this operator acts on
  g: 1.6
  rho: 1.0
  a_sw: 0.4
  hill_n: 4.0
  a0_base: 0.8660254037844386   # base target area (the reset value after division)
- op: shape_energy   # AVM shape-energy force on the vertices (bounded Euler); reads target area a0 from the cell set
  at: vertex   # the set this operator acts on
  p0: 3.72   # target shape index -- the rigidity knob (< ~3.81 solid, > ~3.81 fluid)
  K_A: 1.0   # area-elasticity stiffness
  K_P: 1.0   # perimeter-elasticity stiffness
  mu: 1.0   # mobility (overdamped)
  dt: 1.0   # time step (a label: the bounded-Euler relaxation is per-frame)
  relax_iters: 8   # inner bounded-Euler relaxation steps per frame
  eta: 0.08   # relaxation step size
  cap_frac: 0.15   # max per-step move as a fraction of mean edge (the stability cap)
schedule:   # the order the operators run each frame
- mesh_seed
- cell_seed
- cell_geometry
- cell_morphogen
- morphogen_growth
- shape_energy
