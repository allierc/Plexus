# SMG2 Branching Morphogenesis — Analysis Log

Dated per-batch entries. Every claim pairs a visual observation with an observable number + its
variance share vs the batch's ablation control (quantitative report protocol). Never overwrite prior
batches; append. The variance ledger lives in `knowledge.md` §5.

<!-- batches appended below by the loop -->
