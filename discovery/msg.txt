Overnight work is committed and pushed at 72e3d63. Nothing is running.

The headline
--mode composition could never have completed a round. proposer.propose() requires slot 0 to be intent: "control"; hypothesis.INTENTS didn't contain "control". Two halves of the same protocol disagreed about whether a control exists, and both enforced it with a hard error. Running it attended, as RESUME.md said to, found that plus ten more.

Your Turing + vertex ask
Both minisite videos reproduce again (cells_end 1778 exactly, which validates the clock migration as every: 2→4, not 2→2). Then the loop ran on the settings — 32 runs, 5 knobs, predictions recorded first:

The activator is diluted to extinction. conserve_amount divides cell.chem by the volume growth ratio every tick: 3.3e-2 → 4.6e-11 → 2.0e-22 at frames 60/200/400. The coral pattern on your front page is a colour map stretched over a dead field — which is why both archived movies have identical diagnostics.
With the field alive, rho is the shaping lever, not a_sw — at rho=0.05 the shell clefts along the activator bands, corr +0.63 (best in this project; prior best 0.42).
But: 18 runs with an intact mesh reach at most protr 1.064; all 9 runs with corr > 0.4 have ≥20% folded faces; zero achieve both. Shaping and monolayer integrity are mutually exclusive on this backend. That's a structural limitation, reached by measurement — and it's the same conclusion the earlier campaign reached independently on the SPV backend.
Filed as OR001 in the operator backlog: force-balance to a residual plus 3D IH/HI reconnection, with an acceptance test today's best setting fails by ~4×.

Two things I got wrong, both retracted in the log
I claimed the growth cap explained the flat shape — the first test refuted it, but that test ran against the dead field, so the refutation was worthless. Then I claimed the extinction reached the main campaign; submitted the comparison and measured act_max_final = 0.995 with dilution on. Refuted within the hour — I had the feedback sign backwards. The caption "a red region shrinks and disappears" was about the domain (red_frac 0.005), not the amplitude, and I misread it.

Also closed
Supervisor persistence (it reset every process, which killed budget, escalation triggers and cluster freezing); the surprise-rate scorer (three defects, all biasing toward "nothing learned" — the vcap round's famous 0.40 had never actually been written to the ledger, which said 0.00); the escalation path; straggler kill; reel.py; PDF 21→23pp.

Round 2's Watcher vetoed the two highest-scoring runs, including the control that three Analysts called a tube. The montage shows why: a small body with a short stub.

Next session: round 3 will fire escalation by itself — the Supervisor resumes with dry 1 and its only cluster frozen. Still open: the Proposer blocks the round (~7 min of idle GPUs), analyst calls bypass the LLM budget ledger, and montage tiles aren't on a common spatial scale (labelled, not fixed).