<!-- A STATE DOCUMENT, NOT A LOG. These sections are REWRITTEN IN PLACE every round, never
     appended to. If a section stops being true it is corrected, not annotated -- the history
     lives in analysis.md and hypotheses.jsonl, which are append-only.
     A line earns its place here only if a LATER round needs it and could not re-derive it. -->

# Campaign memory

## Loop semantics
<what a round does here, and anything about THIS campaign's mechanics a fresh session would
get wrong. Rarely changes.>

## Known traps
<things already tried that fail, and the reason. One line each. This is the section that
saves the most compute -- an edit reproposed is a round spent learning nothing twice.>

## Frontier and parent
<what the next round breeds from, and why that and not the alternative.>

## Stability envelope
<the settings a composition must stay inside to be integrable at all, with the measured
numbers. Not a guess -- cite the run.>

## Lessons
<generalisations that survived a round they could have failed in. <=8 lines. Drop one when
adding a ninth; a list nobody can hold is a list nobody reads.>

## Current state
<where the campaign actually is, in five lines: rounds run, what is established, what is open.>

## Next action
<the single thing to do next, and the condition under which it changes.>
